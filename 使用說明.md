# 照片轉影片工具 (photo2video)

用 Rust + egui 寫的桌面小工具，把一連串照片合成一支影片。

## 主畫面的安排

視窗由上往下分成三層（版面比照 Lightroom）：

1. **功能表列**：`檔案(F)`、`編輯(E)`、`模組(M)`、`輔助說明(H)`。開新專案／開啟／儲存、加入照片、檢查更新、關於等「一次性的動作」都收在這裡；最右邊的版本號點下去可開啟「關於」視窗。
2. **模組列**：**左邊是功能模組的切換**——目前有「🎬 照片轉影片」、「💨 去煙霧」、「🎆 煙火疊圖」、「🎥 煙火影片去煙霧」、「✨ 優化影像」與「📂 檔案管理」，點一下就換過去，正在用的那個下面會有一條藍線；右邊顯示程式名稱與目前正在編輯的專案。
3. **工作區**：目前模組自己的畫面（中央預覽、右側設定面板、底部工具列）。

各模組的狀態是**各留各的**：從去煙霧切回照片轉影片再切回來，剛才選的照片、調好的滑桿都還在原樣，不會被清掉。將來新增的功能也會以同樣的方式加成一個模組，出現在模組列上。

**訊息也各歸各的模組**：像「✔ 轉換完成」「✔ 專案已儲存」這類屬於照片轉影片的提示，只會出現在照片轉影片裡，切到去煙霧不會跟著過去。只有程式層級的通知（版本更新、上次閃退回報）才會不分模組顯示在底欄。影片正在轉換時如果切去別的模組，模組列上會留一個小小的「⏳ 轉換中 xx%」，點一下就回到照片轉影片看完整進度。

### 每一條滑桿的數字都可以直接輸入

滑桿右邊那個數字**點一下就變成輸入格**（整串預先選起來，直接打新的數字覆蓋），按 **Enter** 或點到別的地方就套用，**Esc** 取消。輸入格裡按 **↑／↓** 一次加減 1、**Shift+↑／↓** 一次 10，微調到剛好的值不必再用滑鼠慢慢磨；超出範圍的數字會自動夾回該滑桿的上下限，打了看不懂的字就當作沒改。

兩個模組的調色、去煙參數、天空、筆刷（大小、羽化、流暢度、濃度）、遮色片粗細、文字的大小與旋轉都適用；影片的「每秒張數」與字幕的起訖張數本來就能直接打字，用法一樣。

### 資料夾位置會各自記住

每種用途**各記一份**「上次停在哪個資料夾」，開檔與存檔分開、模組之間也不互相干擾——存完影片再去加照片，不會被丟到影片輸出的資料夾；去煙霧的照片來源也不會蓋掉影片專案的位置。目前記錄的有十五個：

| 模組 | 用途 |
| --- | --- |
| 照片轉影片 | 加入照片／選擇資料夾 |
| 照片轉影片 | 開啟與儲存 `.p2v` 專案檔 |
| 照片轉影片 | 選擇背景音樂 |
| 照片轉影片 | 轉檔輸出的影片存放位置 |
| 去煙霧 | 選擇要處理的照片 |
| 去煙霧 | 「另存新檔」的存放資料夾 |
| 煙火疊圖 | 選擇要疊的照片 |
| 煙火疊圖 | 疊圖成品的存檔位置 |
| 煙火影片去煙霧 | 選擇要處理的影片 |
| 煙火影片去煙霧 | 輸出影片的存放位置 |
| 優化影像 | 選擇要處理的照片／資料夾 |
| 優化影像 | 「另存新檔」的存放資料夾 |
| 檔案管理 ▸ 資料備份 | 來源資料夾 |
| 檔案管理 ▸ 資料備份 | 目的資料夾 |

拖曳進來的檔案也算數，下次開對話框就從同一個地方開始。**選資料夾**的對話框會開在上次那個資料夾的**上一層**、並把名字先填好：照片資料夾底下通常沒有子資料夾，直接開在裡面只會看到一句「沒有符合搜尋條件的項目」；開在上一層則同一批的資料夾都看得到，要選同一個也是直接按「選擇資料夾」就好。記錄跟著設定檔存在使用者資料夾，關掉程式再開一樣有效；如果那個資料夾被刪掉、改名或在拔掉的隨身碟上，就當作沒記過，交給系統決定起始位置。

兩個例外照舊：**儲存專案**時若已經有專案檔，會從那個 `.p2v` 自己的資料夾開始（比「上次存專案的地方」更貼近正在做的事）；去煙霧的「💾 存檔」本來就是**存回每張照片自己的原始資料夾**，不會開對話框，也就沒有位置要記。資料備份的那兩個記錄只影響對話框從哪裡開始，畫面上的兩格重開程式一律是空的（理由見〈檔案管理〉）。

### 裁切

四個模組的「**調色**」面板最下面都有一區「**裁切**」，用法完全一樣——調完色再把構圖收一下，不必為了裁一刀再開別的軟體。

按「**✂ 調整裁切範圍**」進入調整狀態，預覽會顯示**整張**照片並蓋上一個裁切框：

- **拖四個角**改兩邊、**拖四條邊**改一邊、**在框內拖**是整個框一起移動。
- 框外會**壓暗**，一眼看得出哪些會被切掉；框內有**三分法格線**，構圖時可以對齊。
- 「**比例**」可選 **自由 / 原圖 / 1:1 / 4:3 / 3:2 / 16:9 / 9:16**，選了就固定成那個長寬比（拖角時另一邊自動跟著算）。按「**⇄ 轉向**」把直式橫式對調（16:9 ⇄ 9:16）。
- 「**旋轉**」有兩種：「**⟲ 90°**」「**⟳ 90°**」是整圈轉（純搬像素，畫質一點都不會掉，直拍轉正用它）；下面的「**拉直**」滑桿是 ±45° 的細角度，把歪掉的水平線轉正（連點兩下歸零）。轉過之後會多一顆「**↺ 轉回 0°**」。
- 轉了之後照片會斜在一個比較大的畫布裡、四角空出來，所以程式會**自動把裁切框縮到剛好塞進照片裡**（維持中心與比例），不會讓你切到那些空白；要再自己微調照樣用拖的。
- 旁邊隨時顯示**裁完會是幾乘幾**（佔原圖的百分之多少、轉了幾度），不必自己換算。

裁好之後**在照片上連點兩下就收工**（手不用離開照片跑回面板；再按一次「✂ 調整裁切範圍」也可以），預覽隨即顯示**裁切後的構圖**（比例列的百分比會跟著變，因為留下來的那塊被放大到填滿畫面）。要整張拿回來就按「**↺ 重設裁切**」。

幾個要點：

- 裁切**存的是相對位置**（佔整張的幾分之幾），不是像素座標。所以預覽是縮圖、存檔是原尺寸，兩邊切到的是**同一塊**；**存檔時才真的切下去，原始照片一律不會被更動**。
- 順序固定是「**先轉再裁**」，而且排在**最後**：去煙、遮色片、手動清除、疊圖、調色做完才轉，轉完才切。遮色片與筆跡的位置是對「原本那張照片」算的，所以排在旋轉之前；文字則是對著轉正後的畫面擺的，排在旋轉之後（所以文字不會跟著歪）。
- 去煙霧模組裡，只要**選著遮色片工具、清除筆刷、吸管，或開著「顯示遮色片」**，預覽就會暫時退回沒轉也沒裁的原圖——那些工具都是照著原圖的位置在畫，畫面轉過就對不上了。工具收起來就會恢復顯示成品。
- 裁切跟著調色走：它和那十二條滑桿記在同一組設定裡，所以**影片模組的個別照片調色（Ctrl+點縮圖多選）也可以只裁那幾張**，專案存檔也會一起記下來。「**↺ 清除所有修改內容**」會把滑桿歸零並**一併取消裁切**。
- 影片模組裁完之後照樣是「等比縮放置中、不足補黑邊」，所以裁成直式的照片在橫式影片裡左右會有黑邊——這與沒裁切時的行為一致。

## 照片轉影片

執行本資料夾中的 `photo2video.exe`（請與 `ffmpeg.exe` 放在同一資料夾），操作步驟：

1. **加入照片**：點「選擇資料夾」或「選擇照片（可多選）」，也可以直接把照片或資料夾拖曳進視窗，或對照片按右鍵用「開啟方式」選擇本程式。照片會依檔名自然排序（img2 會排在 img10 前面），在下方縮圖上按右鍵可移除單張或清空全部，← → 方向鍵可切換預覽。
2. **設定每秒張數 (fps)**：1～60，預設 10，例如 2 代表每秒播 2 張照片；修改後會自動記住，下次開啟直接套用。
3. **調色**：右側面板可調整色溫、色調、曝光度、對比、亮度、陰影、白色、黑色、清晰度、去朦朧、鮮豔度、飽和度（各 -100～+100，**滑桿或左邊的項目名稱連點兩下都可歸零**）。點選下方任一張縮圖即可在中央大預覽區即時檢視效果；預覽與輸出使用同一套 FFmpeg 濾鏡，所見即所得。預設參數套用到影片中的**每一張照片**；想只調特定照片，可按住 **Ctrl 點縮圖**多選（Shift+點可連選一段），此時滑桿只影響選取的照片，有個別調色的縮圖右下角會標示 🎨，也可一鍵「取消個別調色」改回跟隨全部。動過調色後標題旁（多選時在下一列）會出現「**↺ 清除所有修改內容**」，把十二條滑桿一次全部歸零，按下去會先跳確認框。同一區最下面還有「**裁切**」（用法見上面的「[裁切](#裁切)」一節），可以只裁選取的那幾張。
4. **主體追蹤（連拍飛鳥、跑動的人）**：右側「**主體追蹤**」區按一下「⚡ 自動框選全部」，程式就會逐張找出畫面裡快速移動的主體，你再逐一檢查、把不對的自己框起來、不要的照片整批移除；輸出時鏡頭會自動跟著主體跑，**主體一直待在畫面正中央**（用法見下面的「[主體追蹤](#主體追蹤鏡頭自動跟著主體跑)」一節）。
5. **文字（依段落設定）**：按右側「文字」區的「＋ 新增文字」，設定「從第 X 張到第 Y 張」與要顯示的文字（可多行），這段照片就會顯示同一段文字；可新增多段，適合上百張照片的場合。文字可直接在中央預覽區**拖曳移動**到任意位置，點選後可用角落把手縮放、頂端圓形把手旋轉（靠近 45° 的倍數會自動吸附），大小與旋轉角也可在右側用滑桿調整；字型（微軟正黑體、標楷體、Arial 等系統字型）、文字顏色、外框粗細/顏色、半透明底框為全部文字共用；有文字的照片縮圖右上角會標示 💬，點選縮圖可預覽該張的實際效果。
6. **轉場、動態縮放與背景音樂**：「轉場與音樂」區塊可選「淡入淡出」轉場、勾選 Ken Burns 動態縮放（照片緩慢推近/拉遠，啟用任一者輸出會提升為 30fps）；勾選「**結尾淡出**」（預設開啟）則在影片最後一秒把整個畫面（含文字）漸漸壓到全黑，不會播到最後一張就硬生生斷掉——影片本身很短時淡出會跟著縮短，最多只佔全片的三分之一。背景音樂支援 MP3 / WAV / M4A / AAC / FLAC / OGG / OPUS / WMA（也可直接拖進視窗），可調音量與結尾淡出，音樂比影片短會自動循環、比影片長會自動裁切。
7. **選擇輸出格式**：MP4 / MKV / MOV / AVI（H.264）或 WebM（VP9），並可選解析度（720p～4K，或「原始像素」依照片中最大寬高輸出）。照片會等比例縮放置中，不足處補黑邊（調色在補黑邊之前套用，黑邊不會被調亮）。轉換會自動偵測並使用硬體編碼（NVENC / QSV / AMF），沒有可用 GPU 時退回 libx264。
8. **儲存專案 / 開啟舊檔**：`檔案` 功能表的「💾 儲存專案」（快捷鍵 Ctrl+S，工具列也有一顆）會把照片清單與所有設定（每秒張數、解析度、輸出格式、調色含個別照片調色、字幕、轉場、Ken Burns、背景音樂）存成一個 `.p2v` 專案檔；下次用「📂 開啟專案」（Ctrl+O）、`檔案 ▸ 最近的專案`、直接把專案檔拖進視窗，或在啟動畫面的「**最近的專案**」清單點檔名，就能還原上次的工作，不用重新選資料夾、重新設定。「🆕 新專案」則會清空照片與所有設定重新開始。專案檔只記錄照片的路徑，照片本身仍在原資料夾，若照片被移動或刪除，開啟時會提示並略過該張。
9. **還沒加照片時**，底欄的每秒張數、格式、解析度會和「▶ 開始轉換」一起變灰——這些都是「轉這批照片」的設定，加了照片才會亮起來。
10. 按右下角「**▶ 開始轉換**」，選擇儲存位置後開始轉換，底部會顯示進度；轉換途中可按進度旁的「**取消**」隨時中止（會自動刪除未完成的影片，照片與設定都保留）。轉換完成後可按「**▶ 播放**」直接以預設播放器預覽成品，或「**開啟資料夾**」在檔案總管中選取它。右側各設定區塊的標題可點擊收合。

> 本資料夾已附上 `ffmpeg.exe`，不需另外下載；若缺少該檔案，第一次轉換時會自動下載（約 80MB）。

### 主體追蹤：鏡頭自動跟著主體跑

連拍飛鳥、跑動的人、疾駛的車時，主體會在畫面裡從一邊飛到另一邊；照原樣排成影片，主體就在畫面上到處跳。右側「**主體追蹤**」區可以**自動找出主體在每一張的位置**，再**替每一張各切下一塊以牠為中心的畫面**——**主體從頭到尾都待在畫面正中央**，鏡頭像攝影師轉著鏡頭跟拍一樣跟著牠走。

流程就是「**先讓程式框、你再檢查、不要的丟掉**」三步：

**① 自動框選全部**

按「**⚡ 自動框選全部**」，程式會逐張比對「這一張與它前後兩張的差異」，把**自己在動的那一團**找出來，替每一張各框一個主體（進度條會顯示到第幾張，隨時可取消）。133 張大約十秒。

原理是連拍最本質的那個特徵：**主體在動、背景不動**。程式會先估出相機自己的晃動並補掉，再看前後兩張**都覺得不一樣**的地方——主體在前一張留下的殘影、忽然變亮一級的曝光、飄過的一朵雲，都只會讓單邊不一樣，這樣就被濾掉了。

> **為什麼是整批一起挑，不是一張一張各挑各的**：畫面裡會動的東西不只主體（搖晃的樹枝、被風吹的葉子、閃動的光斑）。每張各自挑「最亮的那一團」，就會在牠們之間跳來跳去——那正是成品裡看到的**主體在抖**。所以程式每張留下幾個候選，最後整批一起挑出一條**最連貫**的路徑：代價算的是每一步的「加速度」（軌跡愈平順愈便宜）、大小的變化，再減去「有在移動」的折扣——不減這一項的話反而會偏好原地顫動的葉子（它的加速度幾乎是 0），而這個功能要跟的本來就是會動的主體。實測一段 107 張的翠鳥連拍，軌跡的抖動量（二階差分中位數）從 0.109 降到 0.021，少了五倍。再加上「單張暴衝就用前後三張的中位數換掉」，偶爾認錯的那一張也不會變成畫面上的一次搖晃。

> **主體停下來不動時怎麼辦**：逐張比差異的做法**看不見不動的東西**——牠停在那裡，前後兩張一模一樣。這時程式會做第二趟：**往前或往後找到最近那幾張「已經框到主體」的照片**，拿牠在那裡的樣子當樣板，用「長得像不像」（不是「有沒有在動」）追過來把中間補起來。實測一段 98 張的連拍，中間有二十幾張鳥停在遠處幾乎不動、原本整段都框到樹葉上，補完之後整段都回到鳥身上。判斷「這一段是不是跟丟了」也用得上同一個線索：一整段幾乎不動、這一批其他張卻都在動，那多半是滑到背景上了，程式會把那幾張標成「要檢查」。

**② 逐一檢查、不對的自己框**

按「**🔍 檢查主體框**」，預覽會顯示**整張**照片並畫出這張的主體框；用**滾輪或 ← →** 一張張看過去。縮圖列也一眼看得出狀態：

| 縮圖標示 | 意思 |
| --- | --- |
| （沒有標示） | 框好了，可以直接用 |
| ⚠ 要檢查 | 有框但程式沒把握，請看一眼 |
| ⚠ 沒框到 | 這張找不到主體 |
| ✋ 手動 | 你自己框的 |

框不對就直接在照片上改，三種操作：

- **框內拖曳**＝整個框搬過去（大小不變）——位置差一點時最常用；
- **拖四個角、四條邊**＝改大小（邊中央有小方塊可以抓）；
- **在框外拖曳**＝重畫一個新的框。

改過的那張就變成「✋ 手動」，之後重跑自動框選也**不會被蓋掉**，而且它還會成為補框時的可靠起點。按「**⏭ 下一張要檢查的**」直接跳到下一張有問題的照片；勾「**🔽 只顯示要檢查的**」則讓下面的縮圖列**只排那幾張**，一張一張處理完就自動從那一排消失，不必在上百張裡大海撈針。這張根本沒有主體時按「**✕ 移除這張的框**」，鏡頭會用前後兩張內插滑過去。看完在照片上**連點兩下**收工。

> 自動框選整段都找不到（主體停住不動、背景太亂、或相機正好跟著主體平移）時，改用「**▶ 從手動框追蹤**」：自己框幾張當起點，程式會從那幾張出發一路比對過去把其餘的補起來（比對的是「長得像不像」而不是亮度，曝光跳一級也跟得住）。兩個手動框之間累積的偏差會沿路攤平，所以**多框幾張、每一段各給一個起點**會更準。

**③ 不要的照片一次刪掉**

主體沒入鏡、糊掉、拍到一半被樹擋住的那幾張，通常也不值得留在影片裡：

- 按「**🗑 移除沒框到的 N 張**」一次移除所有「沒框到 ／ 沒把握」的照片（會先確認）。搭配「🔽 只顯示要檢查的」更快：先把那幾張排出來逐一看過，該框的框、該刪的多選起來一次刪掉。
- 自己挑的話，在縮圖列 **Ctrl+點**加選、**Shift+點**連選一段，再按「**🗑 移除選取的 N 張**」（縮圖右鍵選單裡也有一項，或直接按 **Delete** 鍵）。
- 移除的只是「不放進這支影片」，**原始照片一張都不會被刪掉**。

**鏡頭怎麼跟**

兩條滑桿隨時可以調，**調了立刻重算、不必重跑**：

- **鏡頭範圍**（1.5～8 倍，預設 3）：每張切下來的那塊是主體框的幾倍。**倍數小**＝主體大、鏡頭跟得明顯；**倍數大**＝留下較多環境、鏡頭移動較少。旁邊會顯示這樣切下來是**幾乘幾像素**（佔原圖的百分之多少）；小於輸出解析度時會標紅提醒你那是放大來的。

  > **鏡頭範圍開太大，主體反而會在畫面裡飄**：切下來的那塊愈大，能挪動的空間就愈小，主體一往邊緣去鏡頭就頂到照片邊界不能再跟。按「**⤢ 自動選範圍（讓主體都置中）**」可以一鍵反算：要讓每一張的主體都落在正中央，鏡頭最多能開多大。跑完追蹤時若發現超過四分之一的照片會頂到邊界，程式也會**自動幫你調成那個值**並在下面說一聲（不想要就自己再調大，代價是主體會在畫面裡跑動）。
- **平滑度**（0～100）：**0＝主體一格都不差地釘在正中央**（逐張比對的抖動也會跟著上畫面）；調高則鏡頭走得更順，但主體會在中央附近小幅移動。

  > 這條**跑完會自動幫你選一個**，也可以隨時按「**⤢ 自動選平滑度**」重算：程式把「主體偏離中央多少」與「鏡頭自己晃多少」換算成同一把尺（都以成品畫面為單位），取兩者相加最小的那個值。軌跡乾淨的連拍會選到很低的值（主體幾乎完全置中），抓得比較勉強的則自動選高一點把抖動壓下去。

幾個要點：

- **主體一律擺在畫面正中央**。唯一的例外是主體太靠近照片邊緣、鏡頭再往外就會切到照片外面時，鏡頭只能停在邊界上——面板會告訴你有幾張是這樣，**把「鏡頭範圍」調小一點**就能讓它們也置中。
- 追蹤**只認位置、不改大小**：每張切下來的那塊**一樣大**（有手動框時照最大的那個框算，否則取所有框的中位數），否則影片會一直忽遠忽近。那塊的長寬比**跟著輸出解析度**，所以成品不會多出黑邊。
- 結果就是**每張照片自己的裁切框**，所以：套用之後「裁切」區的框不會再套到這些照片上（旋轉、拉直照樣有效）；十二條調色滑桿照常推得動它們；存專案時框與裁切都會一起存下來，下次開檔接著檢查或改參數都可以。
- 要整組取消就按標題旁的「**↺ 清除追蹤**」，每張照片的裁切會**原樣還原**成追蹤前的樣子。
- 追蹤是在**縮小過的灰階圖**上比對（長邊 1024），解照片是整個過程最花時間的一步，程式會同時開三條執行緒先解在前面。

## 去煙霧

煙火照片常被火藥煙擋住——煙被煙火照亮後糊成一大片，夜空發灰、煙火線條也跟著變濁。點左上角模組列的「**💨 去煙霧**」（或 `模組` 功能表）就切到這個模組，可以把這層煙散掉，只留下清晰的煙火線條。

切過去後點「🖼 選擇照片」挑照片（**可一次多選**），也可以直接把照片或資料夾**拖曳進視窗**（在這個模組裡拖進來的照片就是要去煙的那批，不會跑進影片專案）。兩者都是**整批換掉**，所以手上這批還有處理過沒存檔的時候會**先問一次**，選「回去存檔」就回去繼續編輯、存完再換。右側的「調色」與「文字」面板從起始畫面就在，和影片模組的版面一樣；先調好的設定會在照片載進來時直接套上。拖動兩個滑桿即可即時看到效果：

- **去除煙霧**：煙霧要扣掉多少。拉滿煙散得最乾淨，但煙火團之間可能出現偏死黑的區塊。
- **細節**：煙火線條的保留程度。調高時線條更完整，調低則連細線一起柔化。

> **補回煙裡的軌跡**（預設開著）：濃煙很亮時，估出來的煙霧層會比躲在煙裡的軌跡還高，相減把兩者一起扣成全黑——畫面上就是煙火被咬掉一塊。勾著就把「比周圍高出來」的那一截撈回來（「周圍」取的是一小圈裡最暗的那一層，所以連金柳、水面噴泉那種**比取樣窗還大的密集煙火**也認得出來——取平均的話整窗都是它自己，高出量會算成 0，整叢就跟著變細變暗），顏色照原本的像素給，**不是無中生有畫上去的**（把原圖那一塊拉高對比就看得到那些軌跡還在）。純粹是煙的地方高出量接近 0，所以煙照樣去得乾淨（濃煙照片實測去煙量只少 0.24%）。煙紋被撈上來變得斑駁時再把它關掉。

> **去煙預設只作用在天空**（主滑桿下方的「**只處理天空**」勾選，預設開著），地景、建物與水面一個像素都不會動。煙只飄在天空，但岸邊燈火與水面倒影又亮又連續，估起來也像一層「煙」，扣下去整片會被壓暗——所以程式會先自己找出天際線，界線以下完全不處理。煙火簇連同它周圍那團最該清的煙都算天空，不會被當成地景排除掉。煙真的飄到地面、或畫面裡根本沒有地景時，把勾拿掉就整張一視同仁。

煙散掉之後還可以接著**調色**與**疊文字**（見下面兩節），存檔時一次做完寫成一張成品。

### 自動判參數

照片一開進來，程式就會**逐張量一次**，把「去除煙霧、細節、去除雲朵、範圍」四個值直接調到那張照片該有的位置——不必自己從預設值開始試。量的方式是先估出這張照片的煙霧層，再回推「要扣掉多少才能把天空的煙壓回這張照片自己的乾淨夜空」，所以煙濃的那幾張會判得重、天空本來就清朗的判得輕；線條密的煙火會自動調高細節，把線條保得更完整。

- 多張時在背景一張一張量，滑桿旁會顯示還剩幾張；切到哪一張就先量哪一張。
- **動過滑桿的照片就以你調的為準**，之後不會再被自動值蓋回去（檔名旁會標「🎨 個別設定」；沒動過的標「自動」）。
- 調過頭想重來時，按滑桿上方的「**回自動預設值**」就把這張的四個值放回它自己量出來的那一組——**一張一張各自回**，別張不受影響，這張畫好的遮色片、吸好的保護色與雲色、夜空色和調色、文字也都留著。只有動過滑桿的那張才會出現這顆按鈕。（要連遮色片、調色、文字一起歸零則按最下面的「↺ 清除所有修改內容」，按下去會先問一次再清。）
- 不想用就把「**自動判參數**」的勾拿掉，整批回到同一組共用參數（原本的預設值是去除煙霧 80、細節 60）。
- **去除雲朵一開檔就是開著的**（雖然「天空」區塊預設收合，它照樣生效，展開才看得到數值）：即使天空已經被扣乾淨，也會留一個保底的量（20）把殘餘的輝光再壓掉一層，星點與薄雲的層次還在。真的還留著壓不掉的煙霧或雲時，它會自己判得更重，並把「範圍」一起放寬到蓋得到那一片。不想要就把滑桿拉回 0。

「去煙霧」就畫在主視窗裡（右邊是調色與文字面板、中間是預覽），要看大一點就把視窗最大化。**一切進去煙霧，最上面的功能表列與模組列會自己收起來**，整個畫面往上挪、預覽就多出近一百 pixel 的高度；最上緣中間留一條小橫線提醒它們藏在那裡，**游標移到視窗最頂端**兩條列就滑出來，操作完把游標移開又收回去（在功能表選單展開時不會收）。切回「照片轉影片」則照舊一直顯示。視窗拉小、或天空與遮色片選項一起展開而擠不下時，中間的設定區會自己出現捲軸，最下面那排「↺ 清除所有修改內容 / 📁 另存新檔 / 💾 存檔」永遠留在視窗內，不會被裁掉；預覽與設定區之間那條短橫線可上下拖曳改預覽高度，連點兩下回到自動。

預覽下方的**顯示比例**列可以放大細看：「符合視窗」是整張塞進畫面，也能直接點 **50% / 100% / 200%**，或用**滾輪**無段縮放。**按空白鍵直接跳到 100%（1:1）看細節，再按一次回到剛才的比例**（跳之前是「符合視窗」就回符合視窗）；中間若自己用比例列或滾輪改過，那筆記錄就作廢，下次空白鍵回到的是符合視窗。輸入格打字時空白鍵照常是空格，不會亂跳。放大後照片會超出畫面，**沒選遮色片工具時直接用左鍵拖曳預覽**就能移到想看的地方（選了工具時左鍵要留給工具畫遮色片，改用中鍵或右鍵拖曳平移）。

### 只處理部分畫面

去煙本來就只作用在天空（見上一節）。想再縮小範圍——例如只清某幾團煙——有兩種指定方式，可以單獨用也可以一起用：

- **畫遮色片**：在「**遮色片**」那一列選一種工具，直接在照片上拖曳畫出範圍，只有畫到的地方會去煙、其餘原封不動（比照 Lightroom 的遮色片）。四種工具可以**混著用**，怎麼合起來見下面「[遮色片怎麼疊](#遮色片怎麼疊)」。**預設不選任何工具**（＝整片天空都去煙、左鍵用來拖曳預覽），要局部處理時才點一種工具啟用；再點同一個工具一次就關掉。
- **保護某些顏色**：按「**🎨 吸取保護色**」，再到照片上點一下要保住的顏色，與它相近的區域就不會被去煙（類似 Photoshop 依顏色範圍建遮色片）。可以重複吸取**最多 6 個顏色**（例如同時保住橘紅與青綠的煙火），已選的顏色會排成色塊，點色塊即可移除單一個，或按「全部清除」。旁邊的「**容差**」滑桿決定範圍寬窄，數值越大則越多相近的顏色一併被保護。

五種遮色片工具：

- **框選**：拖曳拉出一個方框，只有框內去煙。
- **筆刷**：在照片上塗，塗到的地方才去煙——煙散得不均勻、只想清掉某幾團煙的時候最好用。「**尺寸**」滑桿調筆跡寬度（畫面上會有一個圓圈先讓你看到多粗）。**一筆加一次**：同一塊想更濃，放開滑鼠再塗一遍就好（一次上多少由「筆刷濃度」決定）。
- **線性漸層**：從按下去的那一點是**全效果**，沿著拖曳方向漸弱，到放開的那一點**完全不動**，兩側無限延伸。天空到地面這種「越往下越不該動」的照片一拉就好。
- **放射性漸層**：由中心往外拖出一個橢圓，圓內去煙。勾「**反轉**」則相反，只處理橢圓外——例如把中間的煙火整團保住、只清周圍的煙。
- **物件**：**拖曳框住**要選的東西（一朵煙火、一團煙、一座橋、一棟建築），放開之後程式會自動找出框裡那個東西的**輪廓**——框只是告訴它「東西在這裡面」，貼不貼邊由照片內容決定，不必自己描邊（比照 Lightroom 的「物件」選取）。**框得貼近一點選得越準**，框太大則裡面的東西太雜、容易選錯那一個；每框一次算一個形狀，可以框好幾個疊起來。選好的範圍會沿著輪廓描一圈，想看實際蓋到哪裡就按「**顯示遮色片**」——**與其他工具完全一樣，那顆開關說了算**：關掉之後畫面上什麼都不留，不會只有物件還留著一塊。

  下面兩條滑桿是選完之後才調的，**拉了會直接套到剛框好的那一個**（不必刪掉重框）：

  | 滑桿 | 範圍 | 作用 |
  | --- | --- | --- |
  | **羽化** | 0～100（預設 15） | 邊界往外暈開多寬。0 是硬邊，套回原尺寸時看得出格子的階梯，所以預設先給一點 |
  | **邊緣** | −100～100（預設 0） | 邊界**整圈**往內收（負）或往外擴（正）。選到的東西多了一圈背景就往內收，缺了一小圈就往外擴 |

  選到的範圍會**從最外圍一路填到中心**：大樓沒打燈的那一面、煙火線條之間的夜空，只要是被這個東西整個包住的，**就算全黑也一起選進來**——你要的是「這個東西」整個，不是它亮的那部分。開口通到外面的空缺（例如兩棟樓之間的縫）則維持不選。

框在乾淨的夜空、或框到一整片雜七雜八的地方時，程式分不出「哪個是那個東西」，會提示你重框一次——那是刻意的，否則交出來的會是半片隨機的選取。

畫好後遮色片區會多出兩條滑桿：「**邊緣羽化**」控制邊界的過渡寬度——調 0 會看到一條明顯的接縫，預設 25 讓內外自然銜接（框選是邊框、放射漸層是圓周、筆刷則是筆跡的軟硬）；「**筆刷濃度**」（預設 80）決定**筆刷**一筆上多少——另外四種工具一律 100%，不吃這條。選著筆刷時上面還有一條「**尺寸**」。**這三條在「去煙霧」與「煙火疊圖」是同一組、同一個順序**，兩邊的手感一致。

> 選著「**物件**」時，這兩條會收起來不顯示：物件兩條都不吃——邊界柔不柔由它自己那條「羽化」決定，濃度也一律 100%。擺著只會讓人以為要調，而且畫面上會同時出現「羽化」與「邊緣羽化」兩條更難分。換回別的工具就回來了，值也不會被動到。

### 遮色片怎麼疊

**每畫一個就疊一次**，畫幾個就是它們的聯集；差別只在「一次加多少」：

| 工具 | 一次加多少 |
| --- | --- |
| **筆刷** | 加「**筆刷濃度**」那個值。濃度 80 時塗一筆是八成，同一塊再塗一筆就滿了。同一筆自己繞回來重疊**不會**變濃（否則手抖畫個圈就多出一塊深色），放開滑鼠再塗才算下一筆。 |
| **框選**、**線性漸層**、**放射性漸層**、**物件** | 一律加 **100%**，**不吃濃度**。這幾種是「圈出一塊範圍」，圈到就是要，沒有濃淡可言。 |

所以想要局部深、局部淺，用**筆刷**多塗幾遍；想圈範圍就用另外那四種，圈幾個都會加起來。

疊完之後才看邊界：幾個範圍重疊的地方早就滿了，**只有整片的外緣還留著羽化的過渡**——「邊緣羽化」調的就是那一圈。

**一個形狀都沒畫時濃度不作用**——那代表整片天空都要去煙，不是整片打八折。

**畫好的框選、線性漸層、放射性漸層可以直接拖著搬**：選著任何一種遮色片工具時，已經畫好的形狀會顯示輪廓，游標移上去會變成手掌，按住就整個搬走（大小、比例、方向都不變，放開才重算）。**線性漸層還能轉方向、改寬度**：兩端的圓點是把手，游標移上去會變成指頭，拖它就把那一端拉去別處——方向與長度一起變，要轉角度就拖這裡；拖兩端那兩條橫貫畫面的界線則只沿著漸層方向推（游標是雙箭頭），方向不變、只改過渡帶的寬度。動哪一端另一端都留在原地，放開才重算。想在既有形狀裡面再畫一個，就從它外面開始拖。筆刷的筆跡與「物件」不能搬——那兩種是跟著照片內容畫的，搬走就對不上那團東西了。

畫錯了按「**↩ 還原一個**」拿掉最後畫的那個，或按「**清除遮色片**」全部重來。一張遮色片最多疊 32 個形狀。

畫好的遮色片**平常不會在預覽上留下框線**——那些輪廓是檢查用的，一直蓋在照片上會擋住看成品。**只有勾了「顯示遮色片」才畫出來**（選著工具時也不畫）；正在拖曳的那一個仍有即時回饋，放開之後要確認畫到哪就按「顯示遮色片」。畫好的形狀**要看得到才搬得動**，所以想拖著搬就把它勾起來。

按「**顯示遮色片**」可以把預覽切換成遮罩檢視：**紅色蓋住的地方不會被去煙**，用來確認遮色片與保護色實際涵蓋哪些範圍，再切回來看成品。**還沒畫遮色片、也還沒吸保護色時這顆是灰的**——那時整片天空本來就都會去煙，遮罩檢視只會蓋出一片紅（那是「只處理天空」排除掉的地景，不是你畫的東西），看起來反而像遮色片清不掉。畫好的遮色片被清光時它也會自己關掉，畫面不會卡在遮罩檢視退不出來。

### 去除雲朵、換夜空顏色

「**天空**」區塊（預設收合，點標題展開）還有幾項只針對夜空的調整。這裡的「雲朵」指的是**天空裡沒有煙火紋路的東西**——被煙火或城市燈光照亮的煙霧、殘餘的輝光，以及夜空上真正的雲，都算。判準是「有沒有煙火線條」而不是「亮不亮」：被照亮的煙霧比夜空亮得多，只當暗面處理就永遠碰不到最該處理的那一片。煙火簇連同它自己的光暈、岸邊燈火與地景則靠線條與紋理被排除，而且天空必須從畫面上緣連得過來，所以**煙火、水面與地面不會被動到**：

- **去除雲朵**：把天空裡沒有煙火紋路的煙霧與雲壓回乾淨的夜色，0 是不動。煙火的線條與星點會被保留。這一項與「範圍」也在自動判參數的範圍內：照片一開進來就至少開著保底的 20，天空還留著壓不掉的煙霧或雲時會判得更重。
- **範圍**：多亮的煙霧也算雲朵，數值直接對應亮度（50 就是「亮到一半的煙霧也算」，拉滿則不再看亮度，只看有沒有煙火紋路）。煙霧被照得越亮就要調得越高；如果天空被壓過頭（連該留的薄雲都消失）就往下調。
- **💧 吸取雲色**：有些雲與煙霧邊緣太清楚、紋理太重，連「範圍」拉滿都認不出來。這時按「**💧 吸取雲色**」，再到照片上點那片雲，與它顏色相近的地方就一律當成天空，跟著壓回夜色。可以吸**最多 6 個顏色**（雲的明暗不均時多吸幾處），點色塊移除單一個，或按「全部清除」。旁邊的「**相近**」滑桿決定顏色範圍寬窄——壓不乾淨往上調，連煙火或地景都被壓到就往下調。天空一樣必須從畫面上緣連得過來，所以地面上同色的東西不會被波及。
- **夜空**：點色塊挑一個顏色，天空就會染成那個色調（例如把發灰的夜空換成深藍）。旁邊的「**上色**」滑桿控制濃淡，按「不改色」恢復原本色調。

這幾項也吃前面的遮色片範圍，需要更精確時可以先把天空框起來（或拉一道線性漸層）再調。

### 調色

展開「**調色**」區塊，就有和主畫面同一組的十二條滑桿：**白平衡**（色溫、色調）、**光線**（曝光度、對比、亮度、陰影、白色、黑色）、**質感與色彩**（清晰度、去朦朧、鮮豔度、飽和度）。**每一條的軌道都畫成漸層**，左端就是往 −100 拉會變成的樣子、右端是 +100：色溫是藍→黃、色調是綠→洋紅、去朦朧是霧白→通透的深藍，光線那一組是暗→亮的灰階，鮮豔度與飽和度則是灰→彩虹（飽和度那條更飽和）。**滑桿或左邊的項目名稱連點兩下都可歸零**（名稱比滑桿好瞄，指過去會轉亮），動過之後標題旁會出現「**↺ 清除所有修改內容**」，一次把整組歸零（只動調色，去煙參數、遮色片、文字與手動清除的筆跡都留著）；按下去會先跳確認框，不會誤點就沒了。同一區最下面還有「**裁切**」，用法見上面的「[裁切](#裁切)」一節。

調色是在**去煙之後**才套上去的，所以拖動這些滑桿**不會重跑去煙**——去煙要算上快一秒，調色是即時的，拉起來很順。順序也和主畫面一致（色溫 → 色調 → 曝光 → 對比／亮度／飽和 → 陰影／白色／黑色 → 去朦朧 → 鮮豔度 → 清晰度），同一個數字在兩邊是同一種效果，在這裡調好的心得可以直接搬去調影片。

- **清晰度**是大範圍的局部對比（正值讓煙火線條更立體、負值柔化），半徑會照照片尺寸等比放大，所以**預覽看到的和存出來的是同一種效果**，不會發生「預覽有感、原尺寸卻幾乎看不出來」。
- **去朦朧**（比照 Lightroom 的同名滑桿）處理的是**整張蓋著的一層薄霧**——霧就是一層墊高黑點、把對比與彩度一起壓平的白幕，正值把那層幕減掉再把範圍拉回滿格，負值反過來加一層上去。三個色版一起拉開，所以**顏色也會跟著回來**，不必再補飽和度。
  - 它和上面的「**去除煙霧**」是兩件事：去除煙霧是逐點估煙霧層、認得出煙火線條、還吃遮色片與保護色，專門對付被照亮的濃煙；去朦朧則是整張一視同仁的一刀。**先用去除煙霧把濃煙處理掉，剩下那層均勻的灰再用去朦朧收尾**最順。
  - 拉太滿會把暗部一起壓成死黑（減掉的那層幕最多是 22%），夜景照片通常 30～60 就夠。
- 去煙把煙壓掉之後夜空常會偏平，**黑色**往下拉一點、**對比**加一點就回來了；煙散掉後煙火的顏色通常也會更吃得住**鮮豔度**。

### 手動清除

滑桿是**整張一起算**的，所以零星幾塊沒清乾淨的煙（常見的是天空角落那幾團被照亮的殘煙）只為了它們把「去除煙霧」再拉重，反而會傷到煙火線條。這種時候展開「**手動清除**」區塊，點「**清除筆刷**」，直接在預覽上**塗過去**就好——塗到的地方會用**四周的夜空補起來**，放開滑鼠就看得到結果。

- 「**大小**」滑桿調筆刷粗細；**拖曳**塗一整條、**點一下**清掉一個小點。塗的時候筆跡會亮著，放開就只剩結果，不會留下記號擋住畫面。游標移出照片就變回一般箭頭。
- 游標上有**兩圈淡粉紅**（比照 Lightroom 的筆刷）：**內圈較粗**，是全效果的核心，也就是「這一筆實際清得掉多大一塊」；**外圈細細一條**，是效果淡到 0 的最外緣。兩圈之間那一帶就是羽化的過渡區——拉羽化時看得到它張開或收合，不必真的塗一筆才知道邊緣多柔。羽化拉到 0 時兩圈重疊，只會看到一圈（這時它就是核心，畫的是粗的那條）。
- 另外三條滑桿與 Lightroom 的筆刷同名同義：
  - **羽化**（預設 45）：邊緣多柔。0 是硬邊，塗完會看得到一圈明顯的圓；拉高則由內往外慢慢淡出，接在沒塗到的地方比較自然。
  - **流暢度**（預設 100）：一次上多少。100 是一筆就到底（同一處來回塗不會更濃）；調低則每一次只上一點點，同一個地方多刷幾次才會清乾淨——想慢慢把煙推淡、隨時可以停手時很好用。
  - **濃度**（預設 100）：這一筆最多能清到什麼程度。100 是整塊換成補起來的夜空；調到 50 就只做一半，原本的煙留一半下來——不是要它消失、只是想壓淡一點的時候用。
- 這三條和「保留煙火紋路」一樣**只影響之後才塗的筆跡**，已經塗好的各自記著自己當時的設定，改滑桿不會回頭動到它們。所以同一張照片可以一部分用硬邊全清、一部分用柔邊輕壓。
- 也可以**在照片上按住 Ctrl 滾輪改變筆刷大小**（比照 Photoshop／Lightroom），手不必離開要塗的地方跑去拉滑桿；一格改一成，細的時候微調、粗的時候跨得快。遮色片筆刷也適用。
- 「**保留煙火紋路**」（預設勾著）：煙是一整片平順的、煙火線條與亮芯則是明顯高出來的那一塊，勾著就**只把煙那一層扣掉**，煙火本身一點都不動——煙裡穿過幾條煙火尾巴、甚至整團煙火埋在煙裡時，都可以放心整塊塗過去。取消勾選則是整塊換成補起來的夜空，連煙火一起清掉（要連殘骸、雜物一起抹掉時才用）。
- 這個勾選**只對之後才塗的筆跡生效**：每一筆都記著自己塗下去時的設定，改這裡不會動到已經塗好的那幾筆。所以同一張照片可以一部分保留紋路、一部分整塊清掉。
- 圓圈的**大小是照螢幕算的**，所以**任何顯示比例都能塗**：放大到 100%／200% 再塗，同一圈只蓋到照片上更小的一塊，正好拿來修細節（放大後左鍵留給筆刷，要平移畫面改用中鍵或右鍵拖曳）。
- 補起來的顏色是從筆跡四周取的，而且只會往**暗**的一邊走——清除是把煙拿掉，不會在夜空上多畫出一塊比原本還亮的斑。塗到本來就乾淨的夜空幾乎看不出有動過。
- 筆跡**逐張分開記**，不會像調色、文字那樣套用到整批照片上（塗在某一點只對那一張有意義）。塗過就只剩結果，看得出東西不見了，不必再標記號。
- 標題旁的「**↩ 還原一筆**」拿掉最後塗的那一筆、「**↺ 清除所有修改內容**」清掉這張的所有筆跡（會先跳確認框）；最下面的「↺ 清除所有修改內容」也會一併收掉。筆數沒有上限：塗新的一筆只接著上一筆的結果算，塗到幾百筆預覽也一樣即時（還原或清空時才整串重算一次）。
- 這一段和調色一樣是**去煙之後**才做的，塗一筆**不會重跑去煙**，反應是即時的。順序是**去煙 → 手動清除 → 調色 → 文字**，預覽與存檔完全一致。
- 筆跡存的是相對座標與比例，所以**預覽上清掉的那一塊，存出來也是清掉的那一塊**。

### 疊圖片（logo、標題圖）

展開「**圖片**」區塊，按右上的「**＋ 選擇圖片**」挑一張圖，或按「**📋 貼上**」直接吃剪貼簿裡的圖（Win+Shift+S 截的也行）。加進來之後**直接在預覽上拖曳**擺位置；點一下選起來就會出現把手，**拖四個角縮放**、**拖上方的圓形把手旋轉**（靠近 45 度的倍數會自動吸附），也可以用卡片裡的「大小」「旋轉」「不透明度」滑桿微調。按卡片右上的 🗑 移除那一張。

- **PNG 的透明背景會保留**，所以去背的 logo 疊上去就是去背的樣子。剪貼簿貼進來的則是截圖，透明的地方會變黑——要留透明背景請存成 PNG 再用「選擇圖片」挑。
- 位置、大小（寬度佔照片寬度的百分比）與角度都存成**相對比例**，所以預覽上看到的，存出來就是那樣——存檔時才用原尺寸把圖片真正畫進照片。
- 圖片畫在**調色之後、文字之前**（去煙 → 手動清除 → 調色 → 旋轉 → **圖片** → 文字 → 裁切），所以文字永遠壓在圖片上面，調色也不會動到疊上去的 logo。
- 與文字一樣是**整批共用**的：一次選好幾張照片時，同一個 logo 會疊在每一張的同一個位置上。

### 疊文字

展開「**文字**」區塊、按右上的「**＋ 新增文字**」就會加一段文字（可以加很多段）。在下面的卡片裡打字（**可多行**），然後**直接在預覽上拖曳**就能擺位置；點一下文字會出現選取框，**拖四個角縮放**、**拖上方的圓形把手旋轉**（靠近 45 度的倍數會自動吸附），也可以用卡片裡的「大小」「旋轉」滑桿微調（旋轉滑桿連點兩下歸零）。按卡片右上的 🗑 刪掉那一段。

「**文字樣式（全部共用）**」是所有文字共用的一組設定：**字型**（列出系統裡常見的中文字型）、**外框**粗細、**文字**與**外框**顏色，以及「**半透明底框**」。

- 位置與大小都存成**相對比例**，所以預覽上看到的位置、大小、角度，存出來就是那樣——存檔時才用原尺寸把文字真正畫進照片。
- 大小的基準與主畫面的文字相同（以 1080p 高度為準），所以同樣是 48，套在 1080p 影片和套在原尺寸照片上看起來一樣大。
- 選了只有拉丁字母的字型（例如 Impact）又打中文時，缺的字會自動退回中文字型，預覽不會變成一排方框。
- 文字是**畫在最上層**的（去煙 → 手動清除 → 調色 → 圖片 → 文字），所以遮色片、保護色那些都不會影響它。

### 一次處理多張

選照片時可以一次挑多張（同一場煙火拍的照片，煙霧狀況與構圖通常相近，一組設定就能通用）。載入後預覽下方會出現**縮圖列**，跟主畫面一樣點縮圖就能切換，也可以按 **← →** 方向鍵逐張切換確認效果。

四個數值滑桿由「自動判參數」逐張決定，其餘設定（遮色片、保護色、雲色、夜空色、調色、文字）可以套用到全部照片，也可以只改目前這張——由「**只調整這張**」那個勾決定，而它**預設就是勾著的**：一批照片的煙各有各的濃淡，逐張調本來就是常態，關著的話在第三張上拉一下滑桿，前兩張已經調好的會一起被改掉。想讓一組設定套到整批時再把它取消勾選。有個別設定的照片，檔名旁與縮圖上都會標示「🎨」，按「↩ 這張改回共用設定」即可取消（只想把四個數值滑桿放回自動值、其餘個別設定留著，就按上面的「回自動預設值」）。

去煙的參數與「調色／文字」各記一份個別設定：為了某一張的煙量單獨調過去除強度，**不會**害那張從此收不到整批共用的調色。

再按一次「選擇照片」換一批時，**仍在新清單裡的照片會保留它的個別設定**，所以中途想多加或抽掉幾張，不必重調。**但新選的照片與手上這批完全不重疊時（＝真的換一批）**，畫好的遮色片、吸下來的保護色／雲色／夜空顏色，以及整批共用的調色與文字會一起歸零——那些是對著上一批的畫面畫的、吸的，留著只會在新照片的別的地方生效（天空那一段預設收合，留著的色票還特別看不出來）。去除、細節、羽化、筆刷濃度這些數值滑桿則照舊留著，不必重調。

按「**💾 全部存檔**」就會用每張各自的設定逐張處理，過程中會顯示進度，也可以隨時「取消」（已完成的檔案會保留）。

**只想存其中幾張**時，在縮圖列上用**檔案總管的那套手勢**挑：按住 **Ctrl 點縮圖**一張一張加減、按住 **Shift 點**從目前這張選到點到的那一張（中間整段都收進來）。挑起來的縮圖左上角會出現藍色勾記，存檔鈕跟著變成「**💾 存選取的（N 張）**」，按下去就只處理那幾張。**直接點一下任何一張縮圖**就取消挑選，按鈕變回「全部存檔」——沒挑＝全部，與檔案總管一樣。（只存了一部分時，「尚未存檔」的提醒會繼續亮著，因為沒挑到的那幾張確實還沒存。）

滿意後按「**💾 存檔**」（多張時是「全部存檔」），**直接存回照片原本所在的資料夾**，不用再挑位置；檔名一律是「原檔名_去煙.jpg」，**原始照片不會被更動**（成品檔名剛好與原檔相同時會自動改成「原檔名_去煙(1).jpg」）。多張照片分散在不同資料夾時，每張都各自存回自己那個資料夾。

存檔列上還有「**調整尺寸**」：勾起來就把成品縮到指定的尺寸內（例如 2048 × 1400）再寫檔，**長寬比不變**——是「塞得進那個框」而不是拉成那個比例，長邊先頂到上限、短邊跟著等比縮。「**不放大**」勾著時原圖比指定尺寸還小就維持原樣（放大只會糊掉，不會多出細節）。縮圖用 Lanczos，發文、投稿要指定尺寸時不必再開別的軟體。這個設定**與煙火疊圖共用，也會記住**，下次開程式還在。

**不勾的時候**那一列會寫「原尺寸」，旁邊兩個數字直接顯示**成品實際會是幾乘幾**（原圖尺寸套上旋轉與裁切之後的結果）——不會再留著上次縮到多少的數字，看了以為存出來是那個尺寸。

**成品檔已經存在時會先問一次**（例如上次存過、這次又存一遍）。整批只問一次，不會一張一張煩你：

- **全部覆蓋**＝舊檔直接被蓋掉。
- **另取新檔名**＝保留舊檔，這一批自動改成不重複的檔名（`_去煙(1).jpg`、`_去煙(2).jpg`…）。
- **取消**＝這次不存檔。

想放到別的地方，就按旁邊的「**📁 另存新檔…**」自己挑一個資料夾，該批照片會全部存進去。

存下去的是**去煙 + 調色 + 文字**都做完的成品，一張就到位，不必再拿去別的軟體補。

預覽是縮圖即時運算，存檔時會重讀原檔用完整解析度重跑一次，成品比預覽更銳利，**去煙的程度則一致**：縮圖少了原尺寸估煙霧層時的那一段取樣，同樣的強度會去得比成品乾淨，所以程式在畫預覽前會先照原圖尺寸把強度折算回去——**滑桿上的數字一律代表成品的去煙程度**，不會出現「預覽很乾淨、存出來卻還有一層煙」。遮色片存的是相對位置與比例，換成原尺寸一樣蓋在同一塊。

存完會在旁邊出現「**🖼 開啟圖片**」，按下去就用系統預設的看圖程式開啟剛存好的成品（多張時開第一張），直接檢查完整解析度的效果，不用自己到資料夾裡找。這顆按鈕會留著，直到下次存檔或重新選照片。

**切到別的模組不會清掉這批照片**——切回來就是剛才離開的樣子，滑桿、遮色片、調色與文字都在。**只想拿掉其中一兩張**：在縮圖上按**滑鼠右鍵** ▸「🗑 移除這張」，或先點那張再按 **Delete** 鍵（兩者都會先問一次）。剩下那幾張的個別設定與筆跡都留著，原始照片不會被刪除。

要換一批時按工具列的「**🗑 清除**」（或 `編輯 ▸ 清除去煙霧的照片`）回到「選擇照片」的畫面；若還有處理過沒存檔的照片，會先問一次再清。**清除是真的回到起始狀態**：這一批的去煙參數（共用的與個別的）、**調色與文字**（共用的與個別的）、手動清除的筆跡**全部一起清掉**，下一批就算檔名一樣也不會把舊設定套回去。**只是想增減幾張、設定要留著，就別按清除，直接用「選擇照片」重選**：那條路會保留仍在清單裡那幾張的設定（若新選的照片與這批完全不重疊，遮色片、色票與共用的調色文字仍然會歸零，只留數值滑桿）。

**關閉程式**時，若去煙霧還有處理過、沒存成檔案的照片，會和影片專案的未存檔變更一起提示一次：按「**是**」不儲存直接關閉，按「**否**」留下來繼續調整（只有去煙霧那邊沒存時，會直接幫你切回去煙霧模組）。存檔進行中不能關閉，會請你等這批寫完。

> 去煙霧和影片專案互不相干，兩邊的照片清單各自獨立；處理好的照片若要放進影片，切回「照片轉影片」用「加入照片」加進來即可。

## 煙火疊圖

一發煙火拍下來往往只有一兩朵，畫面總是空了一半。點左上角模組列的「**🎆 煙火疊圖**」（或 `模組` 功能表）切過去，把同一場、同機位拍的好幾張疊成一張「一次放完」的照片——就是 Photoshop 那套「加亮／濾色」疊圖，只是不必開圖層、不必一張一張對位。

> **前提是同機位**：腳架連拍的那一批疊起來才對得準（手持的照片位置會差太多）。各自裁過、尺寸不一樣沒關係——每一層都以原尺寸擺進畫布，再由「地景自動對齊」推到對的位置。

切過去後點「🖼 選擇照片」挑照片（**請選兩張以上**，一次最多 24 張），也可以直接把照片或資料夾**拖曳進視窗**。載入後中間就是疊好的結果，右側是和另外兩個模組同一組的「調色」面板。

**剪貼簿裡的圖片按「📋 貼上圖片」（或 Ctrl+V）就直接變成一層**（**要先有照片**才看得到這顆——貼進來的是「疊上去的一層」，連地景都還沒有時貼它沒有意義）：Win+Shift+S 截的圖、從別的軟體複製的 logo 或標題圖都行，不必先存成檔案。貼進來的圖會存成暫存檔加到清單最後並自動選起來，接著就能拖位置、調縮放；透明的地方會變黑，用「加亮」疊起來剛好只有畫到的地方露出來（程式關掉時暫存檔會自己清掉，要留就先「另存新檔」）。

**要再多疊一張就按「➕ 加入照片」**：它是**加到現在這批後面**，不是整批換掉——已經挑好的照片、畫好的遮色片、調好的擺法與調色全都留著，地景也不會被換掉，**自動對齊只算新加進來的那幾張**（其餘沿用先前算好的結果）。已經有照片時**拖曳進來也是加入**。「🖼 選擇照片」則一律是**整批換掉**；想從頭來過就按「🗑 清除」。**手上這批還沒存檔時，按「選擇照片」會先問一次**：「先存檔」＝存成一張、**存好自動接著開選檔對話框**；「不存檔，直接換」＝這一批的編輯就沒了；「回去編輯」＝什麼都不動。

分**簡易**與**專業**兩種模式，工具列上點一下就切換，**預設是簡易**。

### 簡易模式

只有兩件事要決定：**選哪些照片**、**哪一張當地景**。其餘自動用「加亮」疊起來，調色滿意後存檔即可。調色分兩邊：**這一層**（每一張素材各自的，含地景）與**疊圖後**（整張成品），共用同一組滑桿（見「[調色](#調色這一層與疊圖後兩種模式都有)」）。

**地景**就是當底圖的那一張：它的地面、建物、燈火與水面倒影原封不動留著，其餘每一張只把**比它亮的**部分（也就是煙火）疊上來。挑**地景最乾淨、曝光最準**的那一張當地景，成品的地面就是那一張的樣子。

預設是清單裡的第一張，縮圖左上角會標「**地景**」。要換就點那張縮圖再按「**⬇ 設成地景**」，或直接在縮圖上**連點兩下**。

### 地景自動對齊（預設開啟）

腳架拍的連續曝光之間常差幾個到幾十個像素（按快門的震動、風、雲台鬆動），不校正疊起來整片城市燈火都是重影。**「地景自動對齊」預設就開著**，載入後會在背景把每一張對準地景（旁邊顯示「對齊中… 還有 N 張」）。換地景時會整批重算——對齊是「相對於地景」的。

判斷的方式是比對兩張**都有**的東西（城市燈火、橋樑、天際線）的**輪廓**，煙火的差異會被刻意壓低權重，所以不會變成「讓兩張的煙火重疊」那種錯誤對齊；比輪廓而不是比亮度，兩張的曝光或白平衡不一樣（例如其中一張是修過的版本）也不受影響。

**每張各自在 Lightroom 裁過也對得起來**：那種情形每張的尺寸與長寬比都不同，但它們是同一張感光元件影像的不同裁切——同一個景物點在兩張裡相距的像素數是一樣的。所以程式**不會**為了湊成同樣大小去縮放：**每一層都以原尺寸、一個像素對一個像素擺進畫布**（預設置中），對齊要做的就只是把它推到對的位置。比地景大的部分超出畫布切掉，小的部分四周維持地景。

**水平也會一起校正**：腳架多少會有一點滾動，每張又可能各自拉直過，差個零點幾度而已——但 0.4° 在七千多像素寬的照片上，左右兩端就差了快 30 個像素，看起來就是「怎麼調位移都有一邊對不準」。程式會在畫面左右兩側各挑一塊有結構的地方（城市燈火、橋樑那種）各自量位移，再從兩邊的落差反推角度，解析度大約 0.03°。

> **成品的尺寸就是地景那張的尺寸**，所以地景要挑**最大**（也是構圖最完整）的那一張。疊完之後想再縮放某一層，用下面的「調整圖層」自己調——**縮放完全是你的**，自動對齊不會去動它。

**對不上地景的那幾張會直接略過不疊**：縮圖上標紅色「**對不上**」，設定區顯示「⚠ N 張對不上地景，已略過」。多半是那幾張不是同一個機位、或根本是別場的照片。真的想連它們一起硬疊，把「地景自動對齊」**關掉**即可——關掉之後所有照片都照原樣疊，一張都不會少。

### 地景以下不疊圖（預設開啟）

天際線以下每一張拍到的其實是**同一片地景**，只是各張曝光差一點。用「加亮」疊就是逐點取每一層的最大值，於是整片地面會隨著張數愈疊愈亮——實測疊一層就有六成以上的點被抬亮、平均亮了 18～21 階，燈火糊成一團、暗部的細節也一起被墊高。

**「地景以下不疊圖」預設就開著**：程式自動找出天際線，以下的範圍只留地景那一張的樣子，其餘各層擋掉。

擋的判準**不是位置而是亮多少**，所以兩個例外自然成立：

- **打在地景上空的煙火照樣疊得進來**——它比地景亮上百階，一律放行；只有「亮不到 30 階」的才會被擋掉（那就是同一片地景的曝光差）。門檻上下留有過渡帶，不會出現硬邊。
- **水面倒影不受影響**——平靜的水面是暗而平的、從畫面上緣連得下來，天際線判定會把它算在天空那一側（天際線通常落在海岸線或橋那一帶）。

想連地面一起疊（例如那一批的地景各有各的好，想取最亮的一份），把它**關掉**即可。

> 這與專業模式的**保護區遮色片**是兩回事，可以一起用：這個是自動、看亮度決定放不放行；保護區是你自己畫的，蓋到的地方**誰都疊不上去**（連再亮的煙火也不行）。

### 調色：「這一層」與「疊圖後」（兩種模式都有）

疊圖有兩個東西可以調色，右側面板**最上面一排**切左右，底下**共用同一組十二條滑桿**：

| 選哪一邊 | 調的是什麼 | 什麼時候套用 |
| --- | --- | --- |
| **這一層**（左） | 縮圖列選著的那一張**素材**（**地景也能調**，會標「（地景）」） | **疊進去之前**先套在它身上 |
| **疊圖後**（右） | 疊好的那一張**成品** | **疊完之後**才套用（**裁切**也在這一邊） |

同一場煙火各張的曝光與白平衡本來就有差，疊之前先把各張拉齊，比疊完之後再救整張有效得多：某一張快門慢了半格就把它壓暗一點、某一張偏藍就把色溫拉回來，疊起來才不會一塊亮一塊暗。

- 選「這一層」時，面板上會寫出正在調的是「**第 N 層　檔名**」；要調別層就先在縮圖列點那一張。
- **兩邊會自己跟著你在看的東西切換**：點素材縮圖（或按「單層」）就切到「這一層」，點「疊圖結果」縮圖（或按「疊圖結果」）就切回「疊圖後」。當然也可以直接按那兩顆自己切。
- 切到「**單層**」就看得到這一層調過之後的樣子（疊圖結果同時也會更新）。
- 歸零鈕跟著切：在「這一層」是「**↺ 這層歸零**」（只清這一張）；在「疊圖後」是「**↺ 清除所有修改內容**」（十二條滑桿歸零並取消裁切，各層自己的調色不受影響）。

> 反應速度差在哪：「疊圖後」是疊完才套上去的，拖滑桿**不會重疊一次**，很順；「這一層」動到的是素材，整組會重疊一次，所以慢一點點。

### 調整圖層：搬位置、縮放、旋轉（專業模式）

**每一層都可以自己擺，包含地景。** 先在縮圖列點那一張，再按「**✥ 調整圖層**」，畫面會切到疊圖結果讓你邊看邊調（正在調的那一層縮圖會標「✥ 調整中」，旁邊也會寫出「調整中：第 2 層　檔名」）：

> 正在調的那一層會**框起來**，隨時知道它擺在哪；開著「調整圖層」時**畫面固定停在疊圖結果**，在縮圖列換一層也不會跳去單層檢視（不必再點一次「疊圖結果」才拖得動）。
>
> 對位對不準時再勾旁邊的「**◐ 拖曳時疊上這一層**」（**預設關閉**）：拖曳（或用滾輪縮放、旋轉）的**當下**，那一層會以半透明疊在成品上，暗部也看得到，對起來比較有依據；**手一放開就回到真正的疊圖結果**（放開後還會多留半秒，讓你看清定位的那一眼）。
>
> 為什麼不一直疊著：半透明蓋上去之後，看到的就**不是**真正的疊圖結果了（那一層的煙火加倍亮、地景被壓暗一半），要判斷成品好不好看反而沒依據。
>
> 為什麼需要它：用「加亮」疊出來的成品，只留得住比地景亮的部分，這一層的城市、水面那些**暗的東西根本看不到**——而對位靠的正是那些。

- **移動**：直接在照片上**拖曳**；要對到準確位置就用**鍵盤方向鍵**——調整圖層開著時 **← → ↑ ↓ 一次推 1 px**（按住 Shift 一次 10 px），或用「旋轉」下面那排「**位移**」的兩格數字（左右／上下，單位是像素，＋往右、＋往下；點進去可直接輸入，↑↓ 一次 1 px）。<br>（沒開「調整圖層」時 ← → 仍然是切換選著的那一層。）
- **縮放**：**Ctrl＋滾輪**，或用下面的「縮放」滑桿（20%～300%，連點兩下回 100%）。
- **旋轉**：**Shift＋滾輪**，或用「旋轉」滑桿（±180°，連點兩下歸零）。滑桿右邊的數字框**開放到小數點一位**：點一下直接打「0.3」，或用 **↑↓ 一次加減 0.1**（按住 Shift 一次 1）——多層對位常常就差那零點幾度。

縮放與旋轉都以**畫面中心**為基準，旁邊隨時顯示「位移 +12, −5 px　縮放 100%　旋轉 0.0°」。按「**↺ 這層歸零**」把這一層的三項一次還原（別層不受影響）。

自動對齊與你自己調的是**相加**的：自動對齊只負責把機位的位移抓回來，之後想再搬、再縮、再轉是另一回事，關掉自動對齊也不會弄丟你調的。

**調地景那一層時要知道**：地景同時就是**畫布**，所以搬它、轉它動的是「它的內容在畫布裡的位置」，成品的尺寸不會變，讓開的地方是**黑的**（那底下本來就沒有別的東西，事後用裁切收掉即可）。而且其他各層是對準**地景原本的位置**排好的，所以搬了地景，煙火**不會**跟著走——那正是「每一層各自可調」的意思。想把整張畫面轉正，用「[裁切](#裁切)」裡的拉直，那是對成品做的。

> 只有平移、而且剛好是整數像素時，程式**不會重新取樣**——煙火的細線一點都不會糊。一旦用到縮放或旋轉就得做雙線性取樣，那是必要的代價。

> 這一區是**專業模式限定**。簡易模式每一層只給調色（見上一節），版面才不會一下子塞太多東西。

### 專業模式

簡易模式的功能全部都有，另外多四件事（**「調整圖層」也是其中之一**，見前面那一節）：

**1. 混合方式**

- **加亮**（預設）：兩張逐通道取比較亮的那個。夜空本來就是黑的、煙火是亮的，取大值等於「每一張的煙火都留下來、夜空維持原樣」——**地景不會愈疊愈亮**，張數多也不會失控，煙火疊圖最常用的一種。
- **濾色**：兩張的光相加（但永遠不會超過純白）。煙火互相重疊的地方比加亮更亮、更接近真的重複曝光；代價是**地景、殘煙與夜空也會一起變亮**，張數一多整片就會發灰。兩三張、而且天空夠乾淨時才划算。

**2. 遮色片：擋掉不要疊進來的東西**

工具與去煙霧那邊完全一樣（框選、筆刷、線性漸層、放射性漸層、物件），羽化也共用同一個尺規；差別在**遮色片是畫在「某一張」身上的**：

| 畫在哪一張 | 意思 |
| --- | --- |
| **地景**那一張 | **不能畫**——遮色片那一區在地景上整個是灰的。地景是底圖，在它身上只用「調整圖層」對位 |
| **其他**任何一張 | **這一層只疊圈起來的地方**（預設）。切到「不需疊圖的區域」則相反，變成擋掉那一塊——用來擋這張自己的地景（濾色時特別有用）、或拍壞、位置太擠的那一朵煙火 |

要畫在哪一張，就先在縮圖列點那一張；遮色片區上方會寫清楚現在畫的是哪一張、圈起來算什麼意思。

> 先在某一層畫了遮色片、之後才把那張設成地景的話，那份遮色片仍以保護區生效。這時遮色片區的標題旁會多出一顆「**清除這層遮色片**」讓你清掉，不會卡在那裡清不掉。

**正選或反選**：遮色片那一列右邊有兩顆切換，**逐張各自記**——

| 選哪一邊 | 畫到的地方 | 其餘的地方 |
| --- | --- | --- |
| **選擇疊圖的區域** | **只疊這裡** | 一律不疊 |
| **不需疊圖的區域** | **不疊** | 照疊 |

這兩顆**只有一般的層才有**，預設是「**選擇疊圖的區域**」。

整張裡只想留某一朵煙火時，用預設的「選擇疊圖的區域」把它圈起來就好，比「把其餘全部塗掉」快得多。

> **地景那一張沒有這兩顆**：它只有「保護區」一種用法（圈起來的誰都疊不上去）。地景的遮色片是**全域**的——反過來用的話，只要在地景上畫一個形狀，「其餘所有地方」就一律擋掉別層疊上來，整張瞬間變成沒疊圖。那幾乎不會有人要，卻很容易誤觸，所以乾脆不提供。

> **一個形狀都還沒畫**時，這一層仍然是**整張照疊**（不是整層消失）——否則切過去畫面就莫名少一層，看不出發生什麼事。圈了東西之後才開始生效。

選了任何一種工具，預覽會自動切到「**單層**」——遮色片得對著那一層本身畫，對著疊好的成品畫看不出是誰的。畫完點回「**疊圖結果**」就看得到擋掉之後的樣子。「**顯示遮色片**」把單層檢視切成遮罩檢視：**紅色蓋住的地方這一層不會疊進來**（與去煙霧那顆同一個意思），形狀的輪廓也一併畫出來。**預設開著**；關掉就完全不畫，正在拖的那一筆仍有即時回饋。畫好的形狀**要看得到才搬得動**，所以想拖著搬就把它勾起來。

畫錯了按「**↩ 還原一個**」拿掉最後畫的那個，或「**清除這層遮色片**」把這一張的全部清掉（**別張不受影響**）。一張最多疊 32 個形狀。**畫好的框選與兩種漸層可以直接拖著搬**（游標移上去變成手掌，按住就整個搬走；線性漸層兩端的圓點與界線也能拖——圓點轉方向、改長度，界線只改寬度，與「[去煙霧](#去煙霧)」那邊相同；要在既有形狀裡再畫一個就從它外面開始拖）。

底下兩條滑桿整組遮色片共用：**邊緣羽化**（預設 25）控制疊完那一整片的邊界過渡寬度，調 0 會看到一條明顯的接縫；**筆刷濃度**（預設 80）決定筆刷一筆擋掉幾成。疊起來的方式與去煙霧完全一樣——**每畫一個就疊一次**，只有筆刷吃濃度，框選、漸層與物件一律 100%，詳見上面「[遮色片怎麼疊](#遮色片怎麼疊)」。

> 切回**簡易**模式時，混合方式會回到加亮，遮色片與「調整圖層」調的位移／縮放／旋轉也一律不套用——簡易模式看到的就是「單純把煙火疊起來」的樣子。已經畫好的遮色片、調好的擺法仍留著，切回專業就回來了。**地景自動對齊與各層的調色不受影響**，那兩項兩種模式都照做。

**3. 單層檢視**

比例列左邊多一組「**疊圖結果 / 單層**」。單層是看縮圖列選著的那一張原圖——除了畫遮色片，想確認某一張到底拍到什麼也很好用。

### 縮圖列：素材與成品排在一起

縮圖列除了列出選進來的每一張素材，**最後還多一格「疊圖結果」**（角落有標籤），就是疊好的成品。**點素材縮圖**＝切去看那一張原圖，**點「疊圖結果」**＝回來看成品，兩者來回對照不必去別的地方找開關（比例列的「疊圖結果 / 單層」按的是同一件事）。**← →** 方向鍵一樣可以逐張切換素材。

地景那張的角落標著「**地景**」，有畫遮色片的標 🎨。

### 調色、裁切、檢視與存檔

- **調色**：右側面板那十二條滑桿（白平衡、光線、質感與色彩），最上面一排切「**這一層 / 疊圖後**」，兩邊共用同一組滑桿（見前面的「[調色](#調色這一層與疊圖後兩種模式都有)」一節）。滑桿或項目名稱連點兩下歸零。
- **裁切**：切到「**疊圖後**」，面板最下面就是「裁切」區（用法見前面的「[裁切](#裁切)」一節）。疊圖常常四周會空掉一圈，裁一下構圖就緊實了。
- **檢視**：「**地景／疊圖後**」左右並排比對地景原圖與疊完的樣子（快捷鍵 **Y**）；比例列可選符合視窗 / 50% / 100% / 200%，或用滾輪無段縮放，**空白鍵**在 100% 與剛才的比例之間來回。放大後沒選遮色片工具時直接用左鍵拖曳平移。**← →** 方向鍵切換縮圖列選著的那一張。
- **存檔**：按「**💾 存檔…**」（快捷鍵 Ctrl+S）會先跳存檔視窗讓你**自己挑位置、改檔名**，再**重讀每一張原檔、用完整解析度重疊一次**（不是把預覽放大）。預設檔名取自**目前選著的那一層**（不是地景——地景常常是還沒開始放的那一張，拿它命名看不出這是疊出來的成品），後面加上 `_疊圖`。**同名檔案已存在時會先問要不要覆蓋**；若填的檔名剛好就是來源照片之一，程式會擋下來並提醒換一個，不會把原檔蓋掉。過程中顯示進度百分比，可隨時「取消」。存完旁邊會出現「**🖼 開啟圖片**」直接看成品。 存檔列左邊的「**調整尺寸**」與去煙霧是同一組設定：勾起來就把疊好的成品縮到指定尺寸內再寫檔（長寬比不變、可選「不放大」）。
- 一次只讀一張進來疊，所以就算是兩千萬像素的照片疊上二十張也不會把記憶體吃光。

**切到別的模組不會清掉這批照片**——切回來就是剛才離開的樣子。**只想拿掉其中一兩張**：在縮圖上按**滑鼠右鍵** ▸「🗑 移除這張」，或先點那張再按 **Delete** 鍵（兩者都會先問一次）。剩下那幾張畫好的遮色片、擺法與調色都留著；**移掉的如果是地景**，會改用第一張當地景並重新對齊整批。原始照片不會被刪除，只是不再疊進來。

要換一批時按工具列的「**🗑 清除**」（或 `編輯 ▸ 清除疊圖的照片`）回到「選擇照片」的畫面；若還沒存檔會先問一次。**清除是真的回到起始狀態**：這一批畫的遮色片、每一層搬過的位置／縮放／旋轉、每一層的調色，以及疊圖後的調色與裁切**全部一起清掉**，下一批就算檔名一樣也不會把舊的悄悄套回去。**只是想增減幾張、設定要留著，就別按清除，直接用「選擇照片」重選**：那條路會保留仍在清單裡那幾張的遮色片與擺法。**關閉程式**時若還有疊好沒存的，會和其他模組的未存檔變更一起提示。

> 疊圖和另外兩個模組互不相干，照片清單各自獨立。想先去煙再疊圖的話，先在「去煙霧」把那批處理好存成 `_去煙.jpg`，再到這裡選那批成品來疊。

## 煙火影片去煙霧

錄下來的煙火影片一樣會被煙罩住。點模組列的「**🎥 煙火影片去煙霧**」（或 `模組` 功能表）切過去，選一支影片，程式就把它**一格一格**丟進和照片模組**同一支**去煙演算法裡，再重新編碼成一支新的 MP4——**聲音原封不動搬過去**。

> 用的是同一套演算法，所以同一個數字在照片與影片上是同一種效果；照片那邊調出來的手感直接搬過來用即可。

### 怎麼用

1. 按「**🎥 選擇影片**」挑一支影片，或直接**拖曳進視窗**（支援 MP4／MOV／MKV／AVI／M4V／MTS／M2TS／WMV／WebM／MPG／MPEG）。選好後工具列會顯示這支影片的**解析度、長度、影格率**與**有沒有聲音**。
2. 中央是預覽，底下那條「**時間**」滑桿決定看的是第幾秒。挑幾個煙最濃的時間點，確認設定在整支片子上都合適——不必等整支跑完才發現調過頭。
3. 右側四條設定就是照片模組那四項核心：「**去除煙霧**」「**細節**」「**補回煙裡的軌跡**」「**只處理天空**」（各項的意思見上面「[去煙霧](#去煙霧)」）；底下的「**調色**」區塊可以在去煙之後順手把顏色也調好（見下面「[調色](#調色可套遮色片)」）。

   最上面的「**自動判參數**」預設開著，會照**時間軸現在停的那一格**量出「去除煙霧」與「細節」該有的值，**整支影片共用這一組**——影片不像照片可以一張一張各判各的，逐格判會讓扣掉的量一格一格跳，看起來就是畫面在閃。所以**拖到別的時間點就照那一格重量一次**：挑一格煙最濃的停著，整支跟著它跑最保險。自己動過那兩條滑桿，它就自動關掉、不再蓋回去；想回到自動值再勾回來即可。（清雲與範圍在這個模組沒有介面，自動值不會去動它們。）
4. 預覽下面的「**檢視**」預設是「**原始／去煙後**」：左邊原始畫面、右邊處理後**並排即時對照**，拖滑桿右邊就跟著變，不必按住滑鼠。想看大一點就切「**單張**」（只顯示處理後，**按住畫面**可以看原本的樣子），`Y` 鍵可以在兩種之間切換。專業模式的遮色片畫在右邊那張上。比例列可選「**符合視窗**」或直接點 **50% / 100%**（100%＝成品 1 像素對螢幕 1 個實體像素，與另外幾個模組同一個尺規），也能用**滾輪**在預覽上往上放大、往下縮小（放大後可拖著平移，中鍵或右鍵一律可拖，左鍵在沒選遮色片工具、也沒在調整裁切範圍時也可以）；預覽與下面控制列之間那條短橫線**上下拖曳可調預覽區高度**——往上拖預覽變矮、往下拖變高（拉得比自動配高還高時，下面那幾列改成可以捲），連點兩下回到自動。
5. 「**試播**」：按「**▶ 試播 5 秒**」（旁邊可改 2／5／10 秒）就從目前的時間點抓幾秒、用現在的設定逐格算好，在預覽區**循環播放**——整支輸出動輒幾十分鐘，想看煙散得對不對、有沒有閃爍，先試播就知道。畫面縮到長邊 720、最多 20 格/秒，幾秒鐘就算完；強度照成品尺寸折算，看到的程度與成品一致。播放中可「暫停／繼續」「停止」；改了任何設定會標「設定已變更」，再按一次試播就重新產生（設定沒變則直接重播，不必重算）。10 秒的片段兩份共約 280MB 記憶體。
6. 工具列上的「**簡易／專業**」：程式一開是**簡易**，就是上面這幾項；切到**專業**右側會多一區「**遮色片**」，可以圈出只要去煙的地方（見下面「[簡易／專業與遮色片](#簡易專業與遮色片)」）。
7. 輸出區可選**尺寸**與**品質優先／速度優先**（見下面兩節），再按「**🎬 輸出影片**」挑存檔位置（預設檔名是原檔名加 `_去煙.mp4`），就開始整支跑。
8. 同一場拍了好幾段時，按「**➕ 加入影片**」把其他影片一起排進來（可多選，拖一疊進來也一樣），**全部套同一組設定**一次跑完。排在後面的那幾支會列在工具列下面，點檔名旁的 ✕ 就能拿掉；**點清單裡的檔名就換那一支來預覽**（設定不變），**按住晶片左右拖曳就能自由調整順序**——清單由左到右就是輸出（與合併）的順序，第一支永遠是正在預覽的那支；預覽哪一支，輸出時就先跑哪一支。排了兩支以上時，輸出改成**挑一個資料夾**（不是一支一支問檔名），每支各存成「原檔名_去煙.mp4」，同名時自動加序號；進度會顯示「第 2/5 支」與那一支的檔名，中途某一支失敗不會中斷其他支，最後一起告訴你哪幾支沒成功。

> **合併成一支**：排了兩支以上時，輸出區會多一個「合併成一支」的勾選——勾起來就把這幾支**照清單順序接成一支**影片（只問一個檔名），不勾則各存各的。接的時候**不重新編碼**，所以很快、也不會再掉一次畫質；各段的尺寸或影格率不一致時接不起來，那時會自動改成分開輸出到成品旁邊，跑好的不會白費。

### 簡易／專業與遮色片

工具列上「清除」旁邊有一組「**簡易／專業**」（與煙火疊圖同一個做法），程式一開永遠是**簡易**：

- **簡易**：就是上面那幾項——去煙的四項設定、整張的調色與輸出，整片天空都去煙。
- **專業**：右側多一區「**🎭 遮色片**」，用法與「[去煙霧](#去煙霧)」模組的遮色片**完全相同**：選一種工具（**框選／筆刷／線性漸層／放射性漸層／物件**）直接在預覽畫面上拖曳，**只有畫到的地方會去煙**，其餘一個像素都不動。「**邊緣羽化**」「**筆刷濃度**」、筆刷的「尺寸」、放射性漸層的「反轉」、物件的「羽化／邊緣」，以及「**↩ 還原一個**」「**清除遮色片**」都在；選了工具就會自動打開「**顯示遮色片**」，畫面上**紅色蓋住的地方不會被去煙**。遮罩檢視開著時工具列會標「✏ 編輯中」，按「**完成編輯**」收起來、畫面回到去煙後的樣子（左鍵也回到「按住看原圖」）。專業模式下「調色」區塊也多了**套用遮色片**（見下一節）。

> 形狀存的是畫面的相對位置，**沒分段時整支影片每一格都套同一份**，腳架固定拍的影片這樣就夠了；鏡頭會動的話用下面的「[分段](#分段鏡頭會動時)」，每一段各畫各的。多拉幾個時間點確認圈到的位置一路都對。切回簡易時**不套遮色片**，但畫過的形狀留著，再切到專業就回來。

### 分段（鏡頭會動時）

鏡頭有搖動或改構圖的影片，一份遮色片撐不完整支。專業模式下，時間軸下面多一列「**分段**」：

1. 把時間軸拖到**鏡頭移動之後**的位置，按「**✂ 在此分段**」——從這個時間點起算新的一段，時間軸上會多一條橘色的分界線。
2. 新的一段會**先照抄前一段的遮色片**（鏡頭多半只是移了一點），把框、漸層拖到新位置即可；差太多就按「清除遮色片」重畫。
3. 拖時間軸到哪一段，右側畫的、預覽套的就是**那一段**的遮色片；切了段之後遮色片區塊上面會標「現在畫的是第 2/3 段（…～…）的遮色片」，用「◀ ▶」可以在各段的起點之間跳。
4. 切錯了就把時間軸拖進那一段、按「**✕ 併入前一段**」：這段時間改用前一段的遮色片，這一段畫的就沒了（第一段沒有前一段可併）。

- **每一段各有自己的兩份遮色片**（去煙的與調色的）；去煙的四項設定、調色的滑桿、羽化與濃度仍是**整支共用**——那些跟鏡頭在哪沒關係。
- 輸出時每一格照它落在哪一段套那一段的遮色片，段與段之間是硬切的（分界那一格開始換），所以分界點挑在鏡頭已經停下來的地方最自然。
- 排了好幾支一起跑時，分段的時間點會**照原樣套到每一支**上（那本來就是「同一組設定」的意思）；各支鏡頭動的時間不同的話，一支一支分開跑。

### 調色（可套遮色片）

右側「**調色**」區塊（點標題展開）可以在去煙之後**順手把顏色也調好**，不必再把成品丟進別的軟體。滑桿就是另外三個模組那十二條，用法完全一樣（見「[調色](#調色)」），**整支影片共用同一組**。

特別的地方是它分成**三個遮色區**：「**遮色區 1 / 2 / 3**」各有自己的十二條滑桿與自己那份遮色片，可以**在不同的區域分開獨立調色**（例如一區壓暗地景、一區把夜空壓黑、一區把某一朵煙火拉鮮豔）。

- **一次只顯示一區的設定**：點上面那排「遮色區 1／2／3」切換，切走的那一區照樣記著自己的數字（會標成「**已調**」）。
- **滑桿全歸零的區就不作用**，不必另外關掉；有作用的那幾區照 **1、2、3 的順序依序疊上去**，重疊的地方兩區都會作用。
- 「**↺ 清除這一區**」只把**目前顯示那一區**的十二條滑桿歸零，另外兩區、去煙的設定與所有遮色片都不動。

專業模式下，每一區的滑桿上面都有一個「**套用遮色片**」勾選——勾起來就能**自己圈出這一區要調哪裡、不調哪裡**：

- 先選「畫到的是」哪一種：「**調色的區域**」＝只有畫到的地方調色，其餘一個像素都不動；「**不調色的區域**」＝反過來，畫到的地方**不**調、其餘都調（例如圈住煙火，只調夜空與地景）。
- 底下那排工具（**框選／筆刷／線性漸層／放射性漸層／物件**）、「邊緣羽化」「筆刷濃度」、「↩ 還原一個」「清除遮色片」與「顯示遮色片」，全都與去煙的遮色片同一套用法；差別只在**這是另一份遮色片**——三個遮色區加上去煙那份總共四份，各畫各的、互不影響。**切換遮色區時，正選著的工具會跟著換到那一區**，接下來畫的就是那一區的。
- 一個形狀都還沒畫時，那一區就是整個畫面一起調；畫了第一個形狀才開始只調（或只放過）畫到的地方。
- 遮罩檢視的紅色代表「**不會被調色**」的地方，預覽左上角會寫「遮色區 2 的遮色片（紅色不調色）」，一眼分得出現在看的是哪一份。
- 遮色片是**分段**存的（見上面「[簡易／專業與遮色片](#簡易專業與遮色片)」）：切了段之後，每一段的三個遮色區各有各的形狀；滑桿則是整支共用。
- 調色套在**去煙之後**的畫面上，預覽即時反應（拖滑桿、畫形狀只重跑調色那一段，**不會再等一次去煙**）；有調色時，預覽左上角會寫「去煙霧＋調色後」。

> **會慢多少**：調色是逐格套的，**每多一個作用中的遮色區，每一格去完煙就要多跑一趟調色**。輸出區的說明會寫「調色有 N 區，每一格去完煙還要再跑 N 趟調色」——一支上萬格的片子多一區就是多花不少時間，**先在時間軸上把顏色調滿意再輸出**。4K 又有調色時，「同時處理 N 格」會自動少幾格——調色那一段一格要多攤開不少記憶體。

### 裁切

右側「**裁切**」區塊可以只輸出畫面的一部分（例如把兩邊多餘的岸邊裁掉，或把橫式影片裁成直式）：

1. 按「**✂ 調整裁切範圍**」，預覽改顯示**整格**並畫出裁切框：拖四角、四邊改大小，拖框內移動整個框。
2. 上面會多一排「**比例**」：**自由 / 16:9 / 16:10 / 4:3 / 1:1 / 9:16**，點了就立刻把現有的框調成那個比例（以框的中心為準），之後拖曳也會維持這個比例；「**⇄ 轉向**」把直橫對調（16:9 ⇄ 9:16）。
3. 裁好後在畫面上**連點兩下**（或再按一次「調整裁切範圍」）就收工，預覽改看裁好的那一塊；「**↺ 重設裁切**」回到整格。工具列會標「✏ 編輯中」提醒你裁切框還開著。

- 裁切是在去煙**之前**做的（與縮小同一個道理）：裁掉的地方不必花力氣去煙，輸出也跟著小、跑得也快。旁邊會寫裁完是幾乘幾、佔原本的幾成；「輸出尺寸」的縮放接在裁切之後算（短邊以裁切後的為準）。
- 整支影片（含同一批的其他支）共用同一個框，相對位置套在每一支自己的尺寸上。只裁不轉，原始影片不會被更動。
- 試播與預覽看到的都是裁好的那一塊；正在調整裁切範圍時按試播會先把框收起來。

### 輸出尺寸

輸出區的「**尺寸**」可選 **與來源相同 / 4K（2160p）/ 2K（1440p）/ Full HD（1080p）/ 720p / 480p**，右邊隨時顯示實際會輸出幾乘幾。

- 以**短邊**為準（1080p＝短邊 1080），所以**直拍的影片也會照它該有的方向縮**——1080×1920 選 720p 會得到 720×1280，而不是被壓成橫的。
- **一律不放大**：來源比選的還小就原樣輸出，放大只會讓檔案變大。

重點是 **縮小是在去煙之前做的**——輸出要 1080p 就沒必要先花四倍的力氣把 4K 那一格去完煙再丟掉四分之三的像素。所以選小一號不只檔案變小，**速度也直接跟著快**：

| 4K 來源，輸出 | 速度 | 三分鐘的影片 |
| --- | --- | --- |
| 與來源相同（4K） | 2.5 格/秒 | 約 36 分 |
| Full HD（1080p） | 5.9 格/秒 | 約 15 分 |
| 720p | 12.9 格/秒 | 約 7 分 |

（以上是品質優先；再切到速度優先大約還能再快一倍。）預覽也會跟著縮到成品的尺寸，看到的就是成品的樣子。

### 跑起來要多久

去煙是**逐格算**的，一格花的時間和一張照片差不多，所以整支影片是「格數 × 一格的時間」，只是程式會**同時算很多格**（依 CPU 核心數與影片解析度自動決定，右側會寫「同時處理 N 格」，上限 16 格）。

一格去煙裡最花時間的是估煙霧層、探夜空底色與判天空範圍（八成的時間都在這裡），而這幾樣是**跟著整個畫面慢慢變**的，相鄰幾格幾乎一樣。所以影片不是每一格都從頭估：**60p 的片子每三格估一份、30p 以下每兩格一份，中間的格用前後兩份照距離內插**；跟細節有關的部分（補回軌跡、亮芯的保護、逐像素相減）**仍然每一格自己算**，煙火線條一格都不含糊。畫面變動大的那一格（快速搖鏡、切換畫面）程式會自己看出來，改回從頭估，不會拿對不上的煙霧層去扣。實測 1080p／60fps 的片子從 5.7 格/秒提高到 11.8 格/秒，畫面差異在肉眼看不出來的範圍。

> **為什麼不是核心愈多愈快**：去煙一格要來回搬好幾 GB 的暫存資料，瓶頸在**記憶體頻寬**而不是算力。實測在 16 核／32 緒的機器上，8 條執行緒就跑到 5.8 格/秒、16 條 6.7 格/秒，32 條反而掉回 5.4——再開下去只是互相搶頻寬，還把整台機器佔滿。所以上限訂在 16。

### 品質優先／速度優先

輸出區上面那一組切換，決定**煙霧層要用多細的解析度去估**：

- **品質優先**（預設）：煙霧層在原尺寸估，與「去煙霧」模組處理照片時同一套算法（上面說的隔格內插兩種模式都有）。
- **速度優先**：煙霧層改用比較小的解析度估，**大約快一倍**。滑桿的數字會自動折算，所以**扣掉的煙一樣多**（實測整張的平均亮度只差 0.3%）；差別在煙霧層的細節少一階，煙火線條邊緣附近的殘留會略有不同。縮圖與 100% 檢視都很難分辨，真的要挑才看得出來。

預覽會跟著切換，**先在時間軸上挑一格兩邊比一比**再決定，最實在。

### 跑起來要多久

實測（Ryzen 9 7950X，16 核；1080p／60fps 的實拍煙火，品質優先）：

| 版本 | 速度 | 一分鐘的 60p 影片 |
| --- | --- | --- |
| 每一格都從頭估 | 5.7 格/秒 | 約 10.5 分 |
| 隔格估、中間內插（現在） | 11.8 格/秒 | 約 5 分 |

速度優先大約還能再快一倍；把「尺寸」調小也會跟著快（見上一節）。4K 大約是 1080p 的四分之一速度。

60fps 的片子格數加倍，時間也加倍。核心少的機器會更久。**先用時間軸把設定確認好**再輸出，是最省時間的做法。

輸出完成後右側會出現「**▶ 播放**」（用預設播放器直接看成品）與「**開啟資料夾**」（在檔案總管中顯示它）。

輸出中右側會顯示進度、已經跑了多久與**大約還要多久**（跑得愈久估得愈準）。中途反悔按「**✖ 中止**」，寫到一半的影片會自動刪掉，不會留下一個播不完的檔案。輸出期間預覽暫停更新（CPU 全給輸出），時間軸與四條設定也會鎖住。**輸出還在跑時關閉程式**會先擋下來提醒，請等它跑完或先按中止。

### 幾個要點

- **整支影片套同一組設定**，不像照片模組那樣每張自己量。逐格各自判會讓扣掉的量一格一格跳，看起來就是整片畫面在忽明忽暗；共用一組反而穩。
- **「速度優先」只在影片模組有**。照片一張只要一秒上下，沒有必要為了那一秒換掉品質，所以「去煙霧」模組不提供這個切換。
- 成品是 **H.264 的 MP4**，長寬比與影格率和來源一樣，解析度看「尺寸」怎麼選（旋轉過的手機直拍影片會照它該有的方向輸出）。聲音能直接搬的就直接搬（AAC／MP3／AC3 等），搬不動的才重編成 AAC。
- **不能存回同一個檔案**——那會邊讀邊蓋把來源毀掉，程式會擋下來請你換個檔名。
- 一次處理一支影片。切到別的模組不會清掉它，切回來就是剛才離開的樣子；要換一支按「🗑 清除」（或 `編輯 ▸ 清除影片`）。
- 這個模組做的是**去煙霧與調色**（專業模式可套遮色片），沒有保護色、雲朵清除、手動清除與文字——那些留在照片的「去煙霧」模組裡。

## 優化影像

一整天拍回來的照片，一張一張進 Lightroom 拉滑桿是最花時間的一段。這個模組**每張各自分析**，把那十二條調色滑桿放到那張該有的位置，再自動找出主體另外加強，最後整批另存新檔。**原始照片一律不會被更動。**

### 一、選照片

三種進來的方式，結果一樣：

- 「**🖼 選擇照片**」可一次選多張。
- 「**📂 選擇資料夾**」把整個資料夾的照片一次收進來（**不會**往下找子資料夾）。
- 直接把照片或資料夾**拖曳進視窗**。

照片進來就開始在背景逐張分析（畫面上會顯示「分析中… 還有 N 張」），正在看的那一張會被排到最前面先算。之後想再多加幾張就按「**➕ 加入照片**」——已經調好的設定與正在看的那一張都留著；要整批換掉才用「選擇照片」。

### 二、選類型

| 類型 | 適合 | 調色的走向 | 主體強化 |
| --- | --- | --- | --- |
| **鳥類** | 鳥、昆蟲、小動物，背景是天空或散景 | 通透與對比給得大方 | 預設 65（強） |
| **一般風景** | 山、海、街景，整片畫面都是重點 | 層次、陰影與去朦朧給得足 | 預設 30（輕） |
| **風景人像** | 景裡有人 | 對比與清晰度**刻意收斂**（那兩條專門刻毛孔與皺紋），彩度也壓低 | 預設 45，另外開柔膚 |

> **目前類型列只開放「鳥類」**：一般風景與風景人像暫時不顯示在介面上（整套目標值與柔膚都還在程式裡，之後要放回來隨時可以）。下面兩種的說明先留著備查。

三種用的是**同一套量測**，差別只在目標值。換類型會把受影響的照片重新分析一次（目標值不同，量出來當然不同），你自己調過的那幾張會留著。

**類型是逐張記住的。** 一批照片裡混著鳥、風景與人像是常事，而類型講的是「這張裡面是什麼」（不像強化滑桿是「你想要多少」），所以：

- 勾了「**只調整這張**」再挑類型，就只有這張變，類型列旁邊會出現「🎯 這張自己挑的」，縮圖上也會標 🎨。**切走再切回來，自動顯示它自己的那一個。**
- 沒勾的話是套到整批（四百張都是拍鳥時，總不能一張一張點過去）；**已經自己挑過的那幾張不受影響**，不會被整批的選擇蓋掉。
- 站在「自己挑過」的那張上改類型，改的一律是它自己，不會意外動到整批。
- 要讓某張改回跟整批一樣，按「**↺ 清除這張的修改**」。
- 類型列下面會直接寫這一次會動到誰，點下去之前就看得到。

只有**整批**那一個會記進設定檔，下次開程式的起點是它；個別照片的選擇不會被記走。

### 三、自動調色

打勾就生效（預設開著）。程式在每張照片上量這幾件事，逐項回推成滑桿的值：

- **色偏**（白平衡）：陰天發青、鎢絲燈發橘會被修掉。但**只修一部分**——夕陽、藍調時刻、秋天的樹林本來就整片偏色，硬拉回中性等於把作品的味道洗掉，所以上下限壓在 ±10～12 格。
- **中間調亮度**（曝光度）：拍暗了就提亮、拍亮了就壓回來。
- **反差寬度**（對比）與**暗部有多擠**（陰影）：逆光、樹蔭下的細節就是靠陰影那條撈回來的。
- **最暗與最亮離兩端有多遠**（黑色、白色）：「層次感」說穿了就是黑的夠黑、白的夠白。
- **霧有多厚**（去朦朧）：用暗通道量那層白幕，順便把通透度與彩度一起帶回來。
- **彩度**（鮮豔度、飽和度）：主力給鮮豔度——它對本來就濃的顏色施力小，天空與皮膚不會先爆掉。
- **局部反差**（清晰度）：本來就很紮實的照片再加清晰只會髒掉，所以同樣是往目標值靠。

**刻意的低調與高調照片不會被硬拉回「正常」**：夜景、逆光剪影、黑背景的生態照本來就大半是暗的，程式看中位數與「有多少像素貼在最暗那一端」，越暗就把提亮與抬陰影收得越輕（實測一張夜間煙火照，這一條讓建議值從 +22 曝光 / +30 陰影收回到 0）。雪景、白底商品照則反過來。

「**強度**」是那組建議值要採用幾成：**100＝照建議、50＝一半、200＝加倍**。整批覺得太重或太淡時，動這一條比十二條各拉一次快得多。

右側面板的十二條滑桿顯示的就是**這張實際生效的值**，可以再手動微調——**動任何一條就變成這張的個別設定**，自動值不再蓋回去，縮圖上會標一個 🎨。（自動值本來就是逐張不同的，所以「調色」這一半只可能是在改單張；那兩條強化滑桿才是整批共用的。）

### 四、主體強化與柔膚

程式裡**沒有神經網路**（模型檔比整支程式還大，離線更新也麻煩）。判主體靠的是攝影上「主體長什麼樣」的統計特徵：

- **細節密度**：主體是對到焦的那一塊，高頻能量遠高於天空、水面與散景——拍鳥、拍動物尤其明顯。
- **與周圍的反差**：主體的亮度與顏色和大範圍平均值拉得開（白鳥在藍天前是亮度差、紅花在綠葉前是顏色差，兩邊都算）。
- **膚色**：另外走 YCbCr 的膚色範圍，臉與手直接圈得出來。

三者合成一張權重圖，再**只留連成一片的那幾團**——散落在背景的枝葉、水面反光會被這一步濾掉。

- 「**主體強化**」：主體提亮、加局部對比與銳利、彩度加一成，**背景完全不動**。
- 「**柔膚**」：膚色範圍內的細紋與色斑抹勻。**眼睛、嘴唇、髮際線這些高反差的邊緣會留著**（振幅大的高頻扣得少，所以不會糊成一塊塑膠），而且一定留一成五的紋理。測不到膚色時這條完全不跑。

「自動調色」旁邊會講這張測到什麼（主體佔多少、膚色範圍佔多少）。**膚色那個數字只在柔膚拉起來時才顯示**——柔膚是 0 的時候它對成品沒有任何影響，講了只會誤導：橘紅色的落葉、被燈光打亮的煙，顏色本來就和皮膚落在同一個範圍裡（一張秋天的楓葉照就會報到 13%），但那時候程式並不會對它做任何事。
- **人像模式下，主體強化會自動避開皮膚**：銳利與清晰只加在頭髮、眼睛、衣服上，臉不會被一起銳化——加上「風景人像」的清晰度目標值本來就比另外兩種低，兩件事一起讓臉柔下來。

### 五、看得到才算數

按「**顯示主體範圍**」，預覽就會變成：**主體維持原樣、膚色染粉紅、其餘壓暗染藍**，圈到哪裡一眼就看得出來。

這個檢視不是裝飾——這一套判得出「哪一塊是重點」，判不出「那是什麼東西」。膚色尤其如此：**被燈光打亮的煙與雲，顏色和皮膚幾乎一樣**（所以畫面上一律照實說是「膚色範圍」，不說「測到人物」）。判偏了不會毀掉照片：強化是**依權重漸進**套上去的、不是二值開關，把對應的滑桿調小或歸零就好。

### 六、存檔

- 「**💾 全部存檔**」：每張都存回**它自己的原始資料夾**，檔名加上 `_優化`，不用再選位置（快捷鍵 **Ctrl+S**）。
- 「**📁 另存新檔…**」：自己挑一個資料夾存放整批成品。
- 成品檔已經存在時整批只問一次：**全部覆蓋 / 另取新檔名 / 取消**。
- 旁邊的「**尺寸**」與去煙霧、疊圖共用同一組設定，可把整批縮到指定大小。
- 批次跑到一半可以按「取消」；存完按「🖼 開啟圖片」直接看第一張成品。

### 其他

- 處理順序固定是「**調色 → 主體強化 → 旋轉 → 裁切 → 縮尺寸**」。強化排在調色之後，提亮與加銳才是對著「調完的樣子」做的；預覽與存檔走的是同一條路，只差在尺寸（所有半徑都照長邊等比換算，預覽看到的就是成品的樣子）。
- 快捷鍵與去煙霧一致：**← →** 切換照片、**Y** 前後對照、**空白鍵** 在 100% 與符合視窗之間來回、**Delete** 移除縮圖列選著的那一張（會先問一次）。滾輪縮放、左鍵拖曳平移。
- **柔膚與銳利要放到 100% 以上才看得準**——縮圖大小的預覽本來就看不出毛孔。
- 切到別的模組不會清掉這批照片，切回來就是剛才離開的樣子。要整批重來按「🗑 清除」（或 `編輯 ▸ 清除優化的照片`）；還沒存檔會先問一次。

## 檔案管理

和影像處理無關的雜事收在這裡。模組畫面最上面是一條**功能列**，目前只有「**1. 資料備份**」一項，之後再加的功能會排在它後面。

### 一、資料備份

把來源資料夾同步到目的資料夾：**同名的檔案，來源比較新就覆蓋掉目的那一份；目的沒有的直接拷過去。** 寫的一律是目的那一邊，**來源資料夾從頭到尾都不會被更動**。

1. **選來源與目的**：兩列各按「📂 選擇」挑資料夾，也可以直接把資料夾**拖曳進視窗**（一次拖一個就填由上往下第一個還空著的格子，每一格都滿了就多加一組；一次拖兩個就照順序當成一組「來源、目的」）。

   **來源可以一次選好幾個**：對話框裡按住 `Ctrl` 點選（或 `Shift` 選一整段）就能一次挑好幾個資料夾，**一個資料夾各成一組**，照挑的順序接在這一組後面——要備份的常常是同一顆碟底下好幾個資料夾，不必一個一個開對話框。這一組**已經挑好目的**、而且那個目的是「上一層」（底下會自動套一層與來源同名的資料夾，見下一段）時，新開的那幾組**跟著用同一個目的**，等於一次就排好；目的挑的是「就是這個資料夾」（和來源同名）則不會跟著套，那是為某一個來源挑的。目的一次只挑一個（一組就只有一個目的）。

   **要一次備份好幾個資料夾**：按「**＋ 加入一組**」再多一組來源 → 目的，每一組各挑各的（「含子資料夾」與「保留」兩個開關是全部共用的）。比對與備份會**照順序把每一組都做完**，確認框一組一段；每一組的統計各一列，檔案清單**比完先收著**（動輒幾百列），要看哪一組再按它的「▸ 看清單」，一次只攤一組。哪一組有問題（隨身碟拔掉了、來源在目的裡面、和前面某一組寫到同一個資料夾…）**只跳過那一組**，原因寫在它底下、確認框裡也會列出來，其他組照做。組旁邊的「✖」可以拿掉那一組。

   **重開程式這兩格是空的**，不會把上次的路徑填回來——備份會覆蓋、也可能刪檔，一開程式就已經填好兩個位置，太容易在沒細看的情況下按下去，而那兩個位置未必還是這次想備份的。但**選資料夾的對話框會各自記得上次挑到哪裡**（來源一份、目的一份，見〈資料夾位置會各自記住〉），重挑一次很快。

   **目的挑到上一層時，底下會自動套一層與來源同名的資料夾**：來源挑 `F:\2025_鳥_精選`、目的挑 `O:\`，實際寫入的是 `O:\2025_鳥_精選`，照片不會倒在 `O:\` 的根目錄——同一顆備份碟才放得下好幾組資料夾。這種時候目的那一列下面會用小字把真正寫入的位置寫出來；那個資料夾還不存在也沒關係，**備份時會自動建立**（比對時整批就是「新增」）。

   **直接挑到那一個就用它**：目的挑的是 `O:\2025_鳥_精選`（和來源同名），就直接備份進去，不會變成 `O:\2025_鳥_精選\2025_鳥_精選`。
2. **三個選項**：
   - 「**含子資料夾**」：整棵資料夾一起備份（預設開）。關掉就只比對最上面那一層，子資料夾裡的東西完全不碰。
   - 「**保留目的資料夾多餘的檔案**」：**預設勾著**。勾著時，來源沒有、目的有的檔案原封不動留著；取消勾選則目的資料夾會被整理成和來源一樣，**多出來的檔案會被刪掉**（連同刪空後的資料夾）。刪掉的是**丟進資源回收筒**，不是直接消失；做完之後畫面上會多一顆「**↩ 還原剛才刪除的 N 個檔案**」，按下去就從回收筒放回原位（被收掉的空資料夾也一起建回來）。只認最近一次備份刪的那批；回收筒已經清空的話就放不回來了。
   - 「**不覆蓋目的資料夾較新的檔案**」：**預設勾著**，也是備份本來的規矩——目的那份比來源新就原封不動留著（統計列上的「目的較新（不動）」就是它們），只往目的寫、不會把新的內容換成舊的。

     取消勾選會**先跳一次警告**，確認後那幾個檔案改成用**來源蓋回去**，比對清單上會用紅字標成「**覆蓋較新**」，備份前的確認框也會單獨列一行。什麼時候需要它：目的那邊被別的程式改過（或不小心動到），要整個以來源為準重新蓋一次。

     ⚠ **覆蓋沒有還原這條路**。刪除多餘檔案是丟進資源回收筒、按一下就放得回來；覆蓋掉的內容直接就沒了。所以這一顆平常請維持勾著，真的要以來源為準時再取消。
3. **按「🔍 比對」看看差在哪裡**（可略過）：這一步只是掃描與比較，**不會動到任何檔案**。比對結果畫在**視窗右半邊那一欄**（挑資料夾與按鈕留在左邊，兩邊各自捲動，清單捲到哪裡都不會把按鈕推走）；那一欄的**左邊界可以拖**，路徑長就把它拉寬。每一組上面一列會告訴你這次要做幾件事、要搬多少資料，按「▸ 看清單」列出前 3000 個檔案（做的時候是全部都做）：

   | 清單上的字 | 意思 |
   | --- | --- |
   | **新增** | 目的沒有這個檔，從來源拷過去 |
   | **更新** | 兩邊都有，**來源比較新**，覆蓋掉目的那一份 |
   | **目的地多餘檔案** | 只有目的有。勾了保留就不動，沒勾就刪掉 |
   | **相同** | 兩邊一樣新、也一樣大，不必動（只算數量，不列出來） |
   | **覆蓋較新** | 兩邊都有、**目的那份比較新**，仍然用來源蓋掉它。只有取消「不覆蓋目的資料夾較新的檔案」才會出現，紅字標示——蓋掉就沒了，不進資源回收筒 |
   | **目的較新（不動）** | 兩邊都有，但**目的那份比較新**。備份只往目的寫，所以原封不動留著（只算數量，不列出來）。取消上面那個選項時就沒有這一項了，它們會變成「覆蓋較新」 |

   統計列最後可能還會有一個「**略過 N**」，旁邊的「▸ 看略過的」按下去會把那幾個檔案的完整路徑列出來（來源與目的兩邊都算），想確認到底略過了什麼、在哪裡就看這裡。認的是**名字**，不是隱藏屬性——隱藏不等於不重要：

   | 略過的 | 為什麼 |
   | --- | --- |
   | `._` 開頭的檔案 | Mac 拷貝到外接碟時，每個檔案旁邊留下的附屬檔 |
   | `.DS_Store`、`.fseventsd`、`.Spotlight-V100`、`.TemporaryItems`、`.Trashes` | Mac 的索引與快取 |
   | `Thumbs.db` | Windows 的縮圖快取，刪了會自己重建 |
      | `$RECYCLE.BIN`、`System Volume Information` | 目的挑到磁碟機根目錄時，不能把整個回收筒與系統還原點算成多餘的檔案 |

   除此之外**隱藏檔照樣備份**。Lightroom 目錄庫旁邊那幾個 `xxx Previews.lrdata`、`xxx.lrcat-data` 其實是**資料夾**，整包會照常備份，裡面隱藏的 `desktop.ini` 與 `PackageIcon.ico` 也在內。資料夾的自訂圖示要「資料夾帶系統屬性 ＋ 裡面有 `desktop.ini`」兩個條件同時成立，所以備份時也會把來源資料夾的隱藏／系統屬性一起帶到目的那一份——**每次備份都會對一次**，之前用舊版備出來、屬性不對的資料夾，再跑一次就會補好。唯一的例外是**整個資料夾要被刪掉**的時候（來源沒有它、看得見的東西也刪光了）：裡面剩下的雜檔會跟著一起丟回收筒，資料夾才收得掉，不會留下一個在檔案總管裡看起來是空的、卻怎麼也刪不掉的殼。

   清單上每一行寫的都是**完整路徑**（含磁碟機代號），而且一律是**目的那一邊**的位置——拷過去、蓋過去、刪掉都是在那裡發生。路徑太長時清單可以左右捲。

4. **按「▶ 開始備份」**：**不必先比對**，而且按下去**一定會先重新比對一次**才動手——畫面上那份清單可能是幾分鐘前比的，這期間兩邊的檔案都可能被動過，照舊清單做會拷到已經不存在的檔案、或漏掉剛改過的那幾個。比完會跳一個確認框，把來源、實際寫入的位置與這次要做的每一類事各寫一行——尤其是「刪除」那一件，看清楚再按。確認框的按鈕寫的是**真的要做的事**：只有拷貝就是「開始備份」、只有刪除就是「刪除檔案」、兩者都有就是「備份並刪除」，不會出現按了「開始備份」結果是在刪東西的情況。跑的時候有進度條，隨時可以按「✖ 中止」（已經搬完的那幾個留著，沒搬的下次再比對一次就好）。

**幾件要知道的事**

- **只會寫到目的資料夾**：來源那邊全程唯讀，不會有任何檔案被覆蓋或刪掉。目的那份比較新時就留著它，統計列會顯示「目的較新 N（不動）」讓你知道有這幾個。
- 比對時間差在 **2 秒以內**當成同一份（FAT32／exFAT 的時間戳只有 2 秒解析度，不放寬的話每次備份都要整批重抄一次）。時間一樣但大小不一樣時，以**來源**那份為準補一次——那通常是上次拷到一半留下的。
- 拷過去的檔案**連修改時間一起帶過去**，所以同一組資料夾再備份一次會是「一件都不用做」；不帶的話新拷過去的那份永遠比較新，下一輪整批都會被算成「目的較新」而跳過。
- **來源與目的不能是同一個資料夾，也不能一個在另一個裡面**（那會一層一層拷到磁碟滿），按下去會直接擋掉。
- 捷徑（符號連結／junction）不跟進去；讀不到的資料夾（權限不足、被別的程式鎖住）會跳過並在畫面上提示，不會讓整批停下來。
- 備份還在跑的時候關程式會被擋下來（會留下拷到一半的檔案），請等它跑完或先按「✖ 中止」。
- 刪除一律走**資源回收筒**（Windows 的 `IFileOperation`，和在檔案總管按 Delete 一樣），所以就算沒按「還原」，也還能自己到回收筒撈。放回去時，Windows 因為「隱藏已知檔案類型的副檔名」設定回報的檔名會少了 `.jpg`，程式會從回收筒裡的實體檔案把副檔名補回來，不會還原成沒有副檔名的檔案。
- 「開始備份」右邊那一顆是**把每一組清掉重新挑**（只清畫面上選好的資料夾，不會動到任何檔案）。做的事情一樣，字會看情況換：一批備份**真的跑完之後**寫「**✔ 完成備份**」（這批收工，接著排下一批），其他時候寫「**↺ 重新選擇**」（挑錯了重來）。一個資料夾都還沒挑就不會出現。`編輯 ▸ 清除備份設定` 隨時都能用，做的是同一件事。**「↩ 還原剛才刪除的 N 個檔案」會留著**，按了「完成備份」還是撿得回來。

  按下去時**兩顆會動到檔案的開關會一起回到預設（勾回來）**：「**保留目的資料夾多餘的檔案**」與「**不覆蓋目的資料夾較新的檔案**」。取消它們才會刪檔、才會蓋掉比較新的內容，這種決定只對當時那一批有效；換一批資料夾還默默留著上一批的設定，等於下一批一開始就處在會刪、會蓋的狀態。「含子資料夾」則維持你設的樣子——它只是比對範圍，不會多動到任何檔案。

## 取得最新版

- 最新版直接下載（點了就下載）：<https://github.com/Yellow5363/photo2video/releases/latest/download/photo2video-windows-x64.zip>
- 下載頁面（可看版本更新紀錄）：<https://github.com/Yellow5363/photo2video/releases/latest>
- 程式啟動時會自動檢查更新，視窗底部若出現「⬆ 有新版本」通知，點「**立即更新**」就會自動下載並重新啟動完成更新，不用手動下載。也可從 `輔助說明 ▸ 檢查更新`，或功能表列最右邊的「ℹ 版本號」開啟關於視窗手動檢查。

## 問題回報

- **轉換失敗**時，錯誤訊息旁有「🔗 回報問題」按鈕，點了會開啟回報頁面，並自動帶入錯誤內容與版本，送出即完成回報。
- **程式意外關閉（閃退）**時，下次開啟程式會在視窗底部顯示「⚠ 程式上次異常關閉」通知，同樣點「🔗 回報問題」即可回報。
- **畫面卡住不動**（超過 15 秒沒有反應）時，程式會自動記下當時卡在哪；就算最後是用工作管理員強制結束，下次開啟也會顯示「⚠ 程式上次畫面停止回應」通知，點「🔗 回報問題」把紀錄一起送出即可。

## 命令列模式

也可以不開視窗直接轉換：

```
photo2video.exe --cli <照片資料夾> <fps> <輸出檔>
```

例如：`photo2video.exe --cli D:\photos 2 D:\out.mp4`
（輸出格式由副檔名決定：mp4 / mkv / mov / avi / webm）

命令列模式輸出固定為 **Full HD（1920×1080）**：較大的照片會縮小、直向照片會左右補黑邊。若需要其他解析度（含「原始像素」）、調色、文字或背景音樂，請改用視窗介面。

## 支援的照片格式

jpg / jpeg / png / bmp / webp / tif / tiff

不支援 iPhone 預設的 **HEIC** 格式；可先在手機或電腦上將照片轉存成 JPG 再匯入（iPhone 也可在「設定 → 相機 → 格式」改用「相容性最佳」直接拍成 JPG）。

從 Mac 複製過來的照片資料夾，常會夾帶一批檔名以 **`._`（點加底線）** 開頭的檔案（例如 `._IMG_0001.jpg`）。那是 macOS 自己產生的隱藏附屬檔、不是照片，程式會自動略過，不必手動刪除。
